# 🔑 Хеширане (Hashing)

## 🔑 Какво е хеширане?

Хеширането е процес на преобразуване на данни (напр. низове, масиви) в число с фиксиран размер (хеш код). Основната цел е бързо сравнение и търсене на данни.

**Идеалната хеш функция**:
- Е бърза за пресмятане.
- Минимизира **колизиите** (когато различни данни дават един и същ хеш).

---

## 📝 Полиномно хеширане на низове

Най-често използваният метод в състезателното програмиране.
$$H(S) = (S[0] \cdot P^{n-1} + S[1] \cdot P^{n-2} + \dots + S[n-1] \cdot P^0) \pmod M$$
- $P$: Малко просто число (напр. 31 или 53).
- $M$: Голямо просто число (напр. $10^9 + 7$ или $10^9 + 9$).

### Реализация на C++:
```cpp
long long compute_hash(std::string const& s) {
    const int p = 31;
    const int m = 1e9 + 9;
    long long hash_value = 0;
    long long p_pow = 1;
    for (char c : s) {
        hash_value = (hash_value + (c - 'a' + 1) * p_pow) % m;
        p_pow = (p_pow * p) % m;
    }
    return hash_value;
}
```

---

## 🛡️ Справяне с колизии

1. **Double Hashing**: Пресмятане на два независими хеша с различни модули или бази. Вероятността за колизия и при двата е пренебрежимо малка.
2. **Chaining (в Хеш таблици)**: Използване на списък за елементите с еднакъв хеш.
3. **Open Addressing**: Търсене на съседна свободна клетка.

---

## ⚡ Приложения

1. **Бързо сравнение на поднизове**: Чрез префиксни хешове можем да сравним два произволни подниза на голям текст за $O(1)$.
2. **Хеш таблици**: `std::unordered_map` в C++ предлага средно $O(1)$ достъп.
3. **Алгоритъм на Рабин-Карп**: Търсене на шаблон в текст за $O(N + M)$.
4. **Проверка на изоморфизъм на дървета**: Приписване на хеш на всяко поддърво.

---

## ⚠️ Опасности (Anti-hash тестове)

При използване на фиксиран модул ($10^9 + 7$), "злонамерени" тестове могат да предизвикат колизии.
**Решение**: Използвайте произволна база ($P$), генерирана чрез `rand()` в началото на програмата.

---

## 🏁 Заключение

Хеширането е "швейцарското ножче" на състезателя. То позволява заменянето на бавни сравнения на низове и структури с мигновени проверки на числа, което е критично за оптимизация на времето. 🚀
