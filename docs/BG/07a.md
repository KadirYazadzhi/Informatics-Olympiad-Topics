# 🔍 Алгоритми за сортиране: От основи до оптимизация

Сортирането е една от най-изучаваните теми в информатиката. Различните алгоритми имат различни приложения в зависимост от размера на данните и изискванията за памет.

## 🐢 Прости алгоритми ($O(N^2)$)

### 1. Метод на мехурчето (Bubble Sort)
Най-простият, но най-бавен. Използва се само за много малки масиви ($N < 1000$).
- **Оптимизация**: Ако при един пас не е имало нито една размяна, масивът е сортиран и можем да спрем.

### 2. Сортиране чрез вмъкване (Insertion Sort)
Много ефективен за "почти сортирани" масиви.
- **Принцип**: Вземаме елемент и го "вмъкваме" на правилната му позиция сред вече сортираните пред него.

---

## ⚡ Бързи алгоритми ($O(N \log N)$)

### 1. Бързо сортиране (Quick Sort)
Използва стратегията "Разделяй и владей". Избира се **Pivot** (опорна точка).
- **Важно**: При лош избор на Pivot може да дегенерира до $O(N^2)$. В C++ `std::sort` използва хибриден метод, за да избегне това.

### 2. Сортиране чрез сливане (Merge Sort)
Винаги гарантира $O(N \log N)$.
- **Стабилност**: Запазва относителния ред на равните елементи (важно при сортиране на структури).
- **Памет**: Изисква $O(N)$ допълнителна памет за помощния масив.

---

## 🔢 Не-сравняващи сортировки ($O(N + K)$)

### 1. Counting Sort (Броене)
Ако стойностите са в малък диапазон $[0, K]$.
- Просто броим колко пъти се среща всяко число и после ги изписваме.

### 2. Radix Sort (Поразредно)
Сортира числата цифра по цифра (от най-младшата към най-старшата).

---

## 🏁 Кога кой алгоритъм да ползваме?
- **Стандартно**: Винаги `std::sort()`.
- **Нужна стабилност**: `std::stable_sort()`.
- **Малък диапазон на числата**: Counting Sort.
- **Ограничена памет**: Heapsort или In-place Quick Sort.