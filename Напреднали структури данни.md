# 📂 Напреднали структури от данни

## ⚡ Lazy Propagation (Мързеливо обновяване)
Разширение на сегментното дърво, което позволява обновяване на интервал $[L, R]$ за $O(\log n)$. Използва се масив `lazy[]`, който съхранява промените, които трябва да бъдат "прокарани" към децата по-късно.

---

## 🌳 Сложни дървета

### 1. Merge-Sort Tree
Сегментно дърво, в което всеки възел съхранява **сортиран вектор** от елементите в своя обхват.
- **Приложение**: Преброяване на числа в интервал $[L, R]$, които са по-малки от $X$ ($O(\log^2 n)$ чрез двоично търсене във всеки възел).

### 2. Декартово дърво (Treap)
Хибрид между двоично дърво за търсене и пирамида (Heap). На всеки възел се присвоява произволен приоритет.
- **Предимство**: Самобалансиращо се. Позволява операции като цепене (`split`) и сливане (`merge`) на масиви за $O(\log n)$.

### 3. Персистентни структури (Persistent Data Structures)
Структури, които запазват своите стари версии след обновяване. Най-често се реализират персистентни сегментни дървета, като се създават само нови възли по пътя на промяната.

---

## 🌐 2D Структури (2D Queries)

За задачи в равнината се използват:
1. **2D Сегментно дърво**: Дърво от дървета.
2. **Quadtree**: Всяка клетка се разделя на 4 по-малки.
3. **K-d дърво**: Разделяне на пространството по различни координати на всяко ниво.

---

## 🔤 Trie (Префиксно дърво)

Специализирано дърво за съхранение на низове. Всеки префикс е път от корена.
- **Сложност**: Търсене и вмъкване за $O(\text{length of string})$.
- **Приложение**: Автоматично довършване, речници, XOR задачи (Bits Trie).

---

## ⚖️ Самобалансиращи се BST

1. **AVL дърво**: Поддържа разлика в нивата до 1.
2. **Splay дърво**: При всеки достъп елементът се "изстрелва" до корена чрез ротации. Ефективно за често достъпвани данни.

---

## 🏁 Заключение

Тези структури са "тежката артилерия" в състезателното програмиране. Те позволяват решаването на сложни задачи за обработка на данни в равнината или работа с исторически версии на данни, които иначе биха били нерешими в рамките на времевото ограничение. 🚀
