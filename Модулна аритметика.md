# ➕ Модулна аритметика и приложения

## 📜 Въведение в модулната аритметика

Модулната аритметика е система от аритметични операции за цели числа, при която числата „се завъртат“ след достигане на определена стойност – **модул ($m$)**.

Основни свойства:
1. $(a + b) 
mod m = ((a 
mod m) + (b 
mod m)) 
mod m$
2. $(a - b) 
mod m = ((a 
mod m) - (b 
mod m) + m) 
mod m$
3. $(a \cdot b) 
mod m = ((a 
mod m) \cdot (b 
mod m)) 
mod m$
4. $(a^b) 
mod m = (a 
mod m)^b 
mod m$

**Важно:** Делението в модулна аритметика **не е** директно. Използва се концепцията за **модулно обратно число**.

---

## 🔄 Модулно обратно число (Modular Inverse)

Числото $x$ е модулно обратно на $a$ при модул $m$, ако:
$$a \cdot x 
equiv 1 
mod m$$
Това съществува само ако $\text{gcd}(a, m) = 1$ (числата са взаимно прости).

---

## 📜 Малка теорема на Ферма (Fermat's Little Theorem)

Ако $m$ е **просто число** и $a$ не се дели на $m$, то:
$$a^{m-1} 
equiv 1 
mod m$$
Оттук следва, че модулното обратно число на $a$ е:
$$a^{-1} 
equiv a^{m-2} 
mod m$$

**Пример на C++ (чрез бързо степенуване):**
```cpp
long long power(long long base, long long exp, long long mod) {
    long long res = 1;
    base %= mod;
    while (exp > 0) {
        if (exp % 2 == 1) res = (res * base) % mod;
        base = (base * base) % mod;
        exp /= 2;
    }
    return res;
}

long long modInverse(long long n, long long mod) {
    return power(n, mod - 2, mod);
}
```

---

## 🔁 Разширен алгоритъм на Евклид (Extended Euclidean Algorithm)

Докато стандартният алгоритъм на Евклид намира само $\text{gcd}(a, b)$, разширеният намира и целите числа $x$ и $y$ такива, че:
$$a \cdot x + b \cdot y = 	ext{gcd}(a, b)$$

Това се използва за намиране на модулно обратно число, дори когато модулът **не е** просто число.

### Реализация на C++:
```cpp
long long extendedGCD(long long a, long long b, long long &x, long long &y) {
    if (a == 0) {
        x = 0; y = 1;
        return b;
    }
    long long x1, y1;
    long long gcd = extendedGCD(b % a, a, x1, y1);

    x = y1 - (b / a) * x1;
    y = x1;

    return gcd;
}

long long modInverseEEA(long long a, long long m) {
    long long x, y;
    long long g = extendedGCD(a, m, x, y);
    if (g != 1) return -1; // Не съществува
    return (x % m + m) % m;
}
```

---

## 🛠️ Приложения

1. **Криптография**: Алгоритъмът RSA разчита изцяло на модулната аритметика и намирането на обратни елементи.
2. **Комбинаторика**: Пресмятане на биномиални коефициенти $inom{n}{k} 
mod P$.
   $$\binom{n}{k} = \frac{n!}{k!(n-k)!} 
equiv n! 
cdot (k!)^{-1} 
cdot ((n-k)!)^{-1} 
mod P$$
3. **Хеширане**: Полиномните хешове често използват голям прост модул.

---

## 🏁 Заключение

Модулната аритметика е незаменима при работа с големи числа и вероятности. Овладяването на Малката теорема на Ферма и Разширения алгоритъм на Евклид е задължително за всеки състезател по информатика. 🚀
