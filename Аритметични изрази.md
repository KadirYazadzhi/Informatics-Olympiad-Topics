# ➗ Аритметични изрази: Обработка и пресмятане

## 📝 Представяне на аритметични изрази

В информатиката аритметичните изрази се представят по три основни начина:
1. **Инфиксен запис**: $A + B \cdot C$ (стандартният за хората).
2. **Префиксен запис (полски)**: $+ A \cdot B C$ (операторът е преди операндите).
3. **Постфиксен запис (обратен полски)**: $A B C \cdot +$ (операторът е след операндите).

---

## ⚡ Обратен полски запис (RPN)

Този запис е изключително удобен за пресмятане от компютри, защото не изисква скоби и отчита приоритета на операциите.

### Пресмятане на RPN (чрез Стек):
1. Ако срещнеш число – добави го в стека.
2. Ако срещнеш оператор – извади последните два елемента от стека, приложи оператора и върни резултата обратно.

---

## 🔄 Алгоритъм на Дейкстра (Shunting-yard)

Този алгоритъм преобразува израз от **инфиксен** в **постфиксен** запис.

### Принцип:
- Използва се стек за операторите.
- Числата отиват директно в изхода.
- Операторите се слагат в стека според техния приоритет. Ако новият оператор е с по-нисък или равен приоритет от този на върха, елементите от стека се изваждат към изхода.

---

## 🌳 Дърво на израза (Expression Tree)

Аритметичните изрази могат да се представят като двоични дървета:
- Листата са числа (операнди).
- Вътрешните възли са оператори.
- Коренът е операцията с най-нисък приоритет (която се изпълнява последна).

Обхождането на това дърво:
- **In-order**: Дава инфиксен запис.
- **Post-order**: Дава обратен полски запис.
- **Pre-order**: Дава префиксен запис.

---

## 🛠️ Реализация: Пресмятане на RPN (C++)
```cpp
#include <iostream>
#include <stack>
#include <string>
#include <vector>

int calculateRPN(const std::vector<std::string>& tokens) {
    std::stack<int> s;
    for (const std::string& t : tokens) {
        if (isdigit(t[0])) {
            s.push(std::stoi(t));
        } else {
            int b = s.top(); s.pop();
            int a = s.top(); s.pop();
            if (t == "+") s.push(a + b);
            else if (t == "-") s.push(a - b);
            else if (t == "*") s.push(a * b);
            else if (t == "/") s.push(a / b);
        }
    }
    return s.top();
}
```

---

## 🏁 Заключение

Работата с аритметични изрази е класическа задача за приложение на структурата **Стек**. Разбирането на приоритетите на операциите и преобразуването им в дърво или RPN е ключово за писането на интерпретатори и калкулатори. 🚀
