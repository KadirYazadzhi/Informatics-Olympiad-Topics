# 📊 Анализ на алгоритми. Бързо търсене и бързо сортиране

## ⚖️ Анализ на алгоритми

Анализът на алгоритми е процесът на определяне на количеството ресурси (време и памет), необходими за изпълнението на даден алгоритъм. Това е от критично значение за олимпиадното програмиране, където задачите имат строги ограничения.

### 🕒 Сложност по време (Time Complexity)
Използваме нотацията **Big O (O)**, за да опишем как нараства времето за изпълнение спрямо размера на входните данни ($n$).

- **O(1)**: Постоянно време (напр. достъп до елемент в масив по индекс).
- **O(log n)**: Логаритмично време (напр. двоично търсене).
- **O(n)**: Линейно време (напр. обхождане на масив).
- **O(n log n)**: Линеаритмично време (напр. бързо сортиране, сортиране чрез сливане).
- **O(n²)**: Квадратично време (напр. Bubble sort).
- **O(2ⁿ)**: Експоненциално време (напр. рекурсивно пресмятане на Фибоначи).

### 💾 Сложност по памет (Space Complexity)
Описва колко допълнителна памет заема алгоритъмът. Важно е да се внимава с дълбока рекурсия (Stack Overflow) или големи масиви.

---

## 🔍 Бързо търсене (Двоично търсене)

Двоичното търсене (Binary Search) е алгоритъм за намиране на позицията на търсен елемент в **сортиран** масив.

### Принцип на действие:
1. Намира се средният елемент.
2. Ако той е равен на търсения – задачата е решена.
3. Ако търсеният е по-малък – продължаваме търсенето в лявата половина.
4. Ако е по-голям – в дясната половина.

**Сложност:** $O(\log n)$

### Реализация на C++:
```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int binarySearch(const std::vector<int>& arr, int target) {
    int left = 0;
    int right = arr.size() - 1;

    while (left <= right) {
        int mid = left + (right - left) / 2; // Избягване на препълване

        if (arr[mid] == target) return mid;
        if (arr[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1; // Не е намерен
}

int main() {
    std::vector<int> data = {1, 3, 5, 7, 9, 11, 13, 15};
    int target = 7;
    int index = binarySearch(data, target);

    if (index != -1)
        std::cout << "Елементът е на индекс: " << index << std::endl;
    else
        std::cout << "Не е намерен." << std::endl;

    return 0;
}
```

---

## ⚡ Бързо сортиране (Quick Sort)

Бързото сортиране е един от най-ефективните алгоритми за сортиране. Той използва принципа „Разделяй и владей“ (Divide and Conquer).

### Принцип на действие:
1. Избира се елемент за „опорна точка“ (**pivot**).
2. Масивът се разделя така, че всички елементи, по-малки от pivot, са вляво, а по-големите – вдясно.
3. Двете подмножества се сортират рекурсивно.

**Сложност:**
- Средна: $O(n \log n)$
- Най-лоша: $O(n²)$ (при лош избор на pivot, напр. вече сортиран масив).

### Реализация на C++:
```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int partition(std::vector<int>& arr, int low, int high) {
    int pivot = arr[high]; // Избор на последния за pivot
    int i = low - 1;

    for (int j = low; j < high; j++) {
        if (arr[j] < pivot) {
            i++;
            std::swap(arr[i], arr[j]);
        }
    }
    std::swap(arr[i + 1], arr[high]);
    return i + 1;
}

void quickSort(std::vector<int>& arr, int low, int high) {
    if (low < high) {
        int pi = partition(arr, low, high);

        quickSort(arr, low, pi - 1);  // Сортиране вляво
        quickSort(arr, pi + 1, high); // Сортиране вдясно
    }
}

int main() {
    std::vector<int> data = {10, 7, 8, 9, 1, 5};
    quickSort(data, 0, data.size() - 1);

    std::cout << "Сортиран масив: ";
    for (int x : data) std::cout << x << " ";
    std::cout << std::endl;

    return 0;
}
```

---

## 🆚 Сравнение на алгоритмите за сортиране

| Алгоритъм | Най-добра сложност | Средна сложност | Най-лоша сложност | Стабилност |
| :--- | :---: | :---: | :---: | :---: |
| **Quick Sort** | $O(n \log n)$ | $O(n \log n)$ | $O(n²)$ | Не |
| **Merge Sort** | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ | Да |
| **Bubble Sort** | $O(n)$ | $O(n²)$ | $O(n²)$ | Да |

---

## 🏁 Заключение

Разбирането на сложността на алгоритмите помага да изберете най-подходящия метод за конкретната задача. Двоичното търсене и бързото сортиране са основополагащи инструменти, които се срещат в почти всяко състезание по информатика. 🚀
