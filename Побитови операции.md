# 💻 Побитови операции и техни приложения

## ⌨️ Въведение

Компютрите съхраняват данни в двоичен вид. Побитовите операции позволяват директна манипулация на битовете на целите числа, което е изключително бързо и пести памет.

---

## 🛠️ Основни побитови оператори

| Оператор | Име | Описание | Пример (A=5, B=3) |
| :---: | :--- | :--- | :--- |
| `&` | AND | 1 само ако и двата бита са 1 | `101 & 011 = 001` (1) |
| `\|` | OR | 1 ако поне един от битовете е 1 | `101 \| 011 = 111` (7) |
| `^` | XOR | 1 ако битовете са различни | `101 ^ 011 = 110` (6) |
| `~` | NOT | Обръща всички битове | `~000...101 = 111...010` |
| `<<` | Left Shift | Измества битовете наляво (умножение по 2) | `5 << 1 = 10` |
| `>>` | Right Shift | Измества битовете надясно (деление на 2) | `5 >> 1 = 2` |

---

## 🚀 Полезни трикове и техники

### 1. Проверка на четност
Вместо `n % 2 == 0`, използвайте:
```cpp
if (!(n & 1)) // Четно
```

### 2. Проверка дали число е степен на двойката
```cpp
bool isPowerOfTwo(int n) {
    return n > 0 && (n & (n - 1)) == 0;
}
```

### 3. Работа с $i$-тия бит
- **Включване**: `n |= (1 << i)`
- **Изключване**: `n &= ~(1 << i)`
- **Превключване**: `n ^= (1 << i)`
- **Проверка**: `bool bit = n & (1 << i)`

### 4. Намиране на най-ниския сетнат бит (LSB)
```cpp
int lsb = n & -n;
```

---

## 📊 Битови маски (Bitmasks)

Битовите маски се използват за представяне на малки множества. Ако имаме $N$ елемента, можем да използваме число от $0$ до $2^N - 1$, където $i$-тият бит е 1, ако елементът е в множеството.

### Итериране през всички подмножества:
```cpp
int n = 3; // Множество {0, 1, 2}
for (int mask = 0; mask < (1 << n); mask++) {
    for (int i = 0; i < n; i++) {
        if (mask & (1 << i)) {
            std::cout << i << " ";
        }
    }
    std::cout << std::endl;
}
```

---

## 🏗️ Приложения в алгоритмите

1. **Динамично програмиране с маски (DP on masks)**: Решаване на задачи като Търговския пътник (TSP) за малък брой градове.
2. **Фенуик дърво (Binary Indexed Tree)**: Използва `n & -n` за бърза навигация.
3. **Оптимизация на паметта**: Съхраняване на булеви флагове в едно цяло число.
4. **Криптография**: XOR се използва масово в шифри.

---

## 🏁 Заключение

Побитовите операции са "магията", която прави алгоритмите по-бързи. Те са задължителни за задачи, изискващи работа с подмножества или оптимизация на ниско ниво. 🚀
