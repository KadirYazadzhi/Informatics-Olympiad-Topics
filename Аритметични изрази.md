# ➗ Аритметични изрази: Парсване и Оценка

## 📝 Парсване чрез рекурсивно спускане (Recursive Descent Parser)

Това е най-гъвкавият метод за обработка на изрази с различни приоритети, скоби и функции. Дефинираме граматика и пишем функция за всяко ниво на приоритет.

### Граматика (Примерна):
- **Expression**: Term + Term | Term - Term
- **Term**: Factor * Factor | Factor / Factor
- **Factor**: (Expression) | Number | -Factor

### Реализация:
```cpp
const char* p; // Текуща позиция в низа

int expression();
int factor();

int term() {
    int left = factor();
    while (*p == '*' || *p == '/') {
        char op = *p++;
        int right = factor();
        if (op == '*') left *= right;
        else left /= right;
    }
    return left;
}

int expression() {
    int left = term();
    while (*p == '+' || *p == '-') {
        char op = *p++;
        int right = term();
        if (op == '+') left += right;
        else left -= right;
    }
    return left;
}

int factor() {
    if (*p == '(') {
        p++;
        int val = expression();
        p++; // skip ')'
        return val;
    }
    // Четене на число
    int val = 0;
    while (isdigit(*p)) {
        val = val * 10 + (*p - '0');
        p++;
    }
    return val;
}
```

---

## ⚡ Оценка на логически изрази

Същият подход се прилага за булеви изрази (`&&`, `||`, `!`).
- Най-нисък приоритет: `||`
- Среден приоритет: `&&`
- Най-висок приоритет: `!` и `()`

Важно е да се реализира "lazy evaluation" (късо съединение) – ако лявата страна на `&&` е `false`, дясната не се оценява (важно, ако дясната страна има странични ефекти или е невалидна).

---

## 🏁 Заключение

Recursive Descent е много по-лесен за разширяване от Shunting-yard. Ако задачата изисква поддръжка на функции `sin()`, `max()`, или променливи, рекурсивният парсер е правилният избор.