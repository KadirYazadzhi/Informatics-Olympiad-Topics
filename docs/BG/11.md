# 🧭 Начални познания по компютърна геометрия

Компютърната геометрия се занимава с алгоритми за обекти като точки, отсечки, прави и полигони.

## 📍 Точка и Вектор
Най-важната структура. Векторът е насочена отсечка от $(0,0)$ до $(x,y)$.

```cpp
struct Point {
    double x, y;
    Point operator-(const Point& other) const { return {x - other.x, y - other.y}; }
};
```

## 📏 Скаларно и Векторно произведение

### 1. Скаларно произведение (Dot Product)
$$\vec{A} \cdot \vec{B} = x_A x_B + y_A y_B = |A||B| \cos \theta$$
- Ако е 0 $\to$ векторите са перпендикулярни.
- Помага за намиране на проекция.

### 2. Векторно произведение (Cross Product)
$$A \times B = x_A y_B - x_B y_A$$
- Дава ориентираната площ на паралелограма.
- **Най-важното**: Посоката на завоя.
  - $>0$ $\to$ завой наляво.
  - $<0$ $\to$ завой надясно.
  - $=0$ $\to$ колинеарни (на една права).

---

## 🏁 Заключение
Геометрията изисква прецизност. Винаги, когато можете, работете с цели числа (`long long`), за да избегнете грешки от закръгляне на `double`.