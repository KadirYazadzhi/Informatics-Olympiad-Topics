# 📐 Основи на Изчислителната Геометрия

Геометрията е една от най-предизвикателните теми в състезателното програмиране заради работата с числа с плаваща запетая (`double`) и граничните случаи.

## 1. Точки и Вектори

Точка $P(x, y)$ и вектор $\vec{v}(x, y)$ се представят еднакво, но имат различен смисъл.
*   **Точка**: Позиция в пространството.
*   **Вектор**: Посока и големина (разлика между две точки).

```cpp
struct Point {
    long long x, y; // Използвайте long long винаги, когато е възможно!
    
    Point operator+(const Point& other) const { return {x + other.x, y + other.y}; }
    Point operator-(const Point& other) const { return {x - other.x, y - other.y}; }
};
```

## 2. Скаларно Произведение (Dot Product)
$A \cdot B = x_A x_B + y_A y_B = |A| |B| \cos \theta$
*   Ако $A \cdot B > 0$: Ъгълът е остър ($< 90^\circ$).
*   Ако $A \cdot B = 0$: Векторите са **перпендикулярни**.
*   Ако $A \cdot B < 0$: Ъгълът е тъп ($> 90^\circ$).

```cpp
long long dot(Point a, Point b) {
    return a.x * b.x + a.y * b.y;
}
```

## 3. Векторно Произведение (Cross Product)
В 2D това е число (в 3D е вектор):
$A \times B = x_A y_B - x_B y_A = |A| |B| \sin \theta$
*   Геометрично: Ориентираното лице на успоредника, образуван от $A$ и $B$.
*   Лице на триъгълник с върхове $(0,0), A, B$: $\frac{|A \times B|}{2}$.

```cpp
long long cross(Point a, Point b) {
    return a.x * b.y - a.y * b.x;
}
```

## 4. Ориентация на три точки (CCW)
Къде се намира точка $C$ спрямо правата $AB$?
Използваме векторното произведение на $\vec{AB}$ и $\vec{AC}$.
$CP = (B.x - A.x)(C.y - A.y) - (B.y - A.y)(C.x - A.x)$

*   $CP > 0$: $C$ е **вляво** от $AB$ (обратно на часовника - CCW).
*   $CP < 0$: $C$ е **вдясно** от $AB$ (по часовника - CW).
*   $CP = 0$: $A, B, C$ са **колинеарни** (лежат на една права).

```cpp
long long orient(Point a, Point b, Point c) {
    return cross(b - a, c - a);
}
```

## 5. Разстояния
*   Евклидово разстояние: $\sqrt{(x_1-x_2)^2 + (y_1-y_2)^2}$.
*   Манхатън разстояние: $|x_1-x_2| + |y_1-y_2|$.

## 6. Задачи за упражнение
1.  **Codeforces 1C**: Ancient Berland Circus (Лице и радиуси).
2.  **UVa 191**: Intersection (Пресичане на отсечки).
3.  **Convex Hull**: (Виж тема 27, изисква Cross Product).

## 🏁 Съвети
1.  Избягвайте `double` и `sqrt`, когато сравнявате разстояния. Сравнявайте квадратите им (`distSq`).
2.  Ако се налага `double`, използвайте `EPS` (епсилон) за сравнение: `abs(a - b) < 1e-9`.
```