# 🌐 Алгоритми върху графи: Разширени методи

## 🛣️ Най-къси пътища - Специални случаи

### 0-1 BFS
Ако теглата на ребрата са само 0 и 1, можем да намерим най-къс път за $O(V+E)$ използвайки `deque`.
- Ако теглото е 0: `push_front`
- Ако теглото е 1: `push_back`
Това запазва свойството на опашката да е сортирана по разстояние.

### SPFA (Shortest Path Faster Algorithm)
Подобрение на Белман-Форд, което на практика често работи за $O(kE)$ ($k$ е малка константа), но в най-лошия случай е $O(VE)$.
- Използва опашка за върховете, чието разстояние се е променило (релаксирани).
- **Внимание**: Лесно се създават "killer tests", които го забавят. За предпочитане е Дейкстра, освен ако няма отрицателни тегла.

### Алгоритъм на Джонсън (Johnson's Algorithm)
Намира всички двойки най-къси пътища в граф с отрицателни тегла (без отрицателни цикли).
1. Добавя се фиктивен връх $S$, свързан с всички с тегло 0.
2. Изпълнява се Белман-Форд от $S$, за да се намерят потенциали $h(v)$.
3. Ребрата се претеглят: $w'(u, v) = w(u, v) + h(u) - h(v) \ge 0$.
4. Изпълнява се Дейкстра $V$ пъти.
- Сложност: $O(V E \log V)$.

---

## 🌳 Център и Диаметър на дърво

### Диаметър
Най-дългият път между два върха в дърво.
**Алгоритъм (2 пъти BFS/DFS)**:
1. Започваме BFS от произволен връх $u$ и намираме най-далечния връх $x$.
2. Започваме BFS от $x$ и намираме най-далечния връх $y$.
3. Разстоянието между $x$ и $y$ е диаметърът.

### Център
Връх (или два съседни), който минимизира максималното разстояние до всеки друг връх.
- Намира се като средната точка на диаметъра.

---

## 🔍 Lowest Common Ancestor (LCA)

Най-ниският общ прародител на два върха $u$ и $v$ в коренно дърво.
**Метод с Binary Lifting (Двоично повдигане)**:
- `up[u][i]` е $2^i$-тият прародител на $u$.
- Пресмятане: `up[u][i] = up[up[u][i-1]][i-1]`.
- Търсене на LCA:
  1. Изравняваме дълбочините на $u$ и $v$.
  2. "Повдигаме" двата върха едновременно с големи стъпки ($2^k$), докато не станат децата на LCA.
  3. `up[u][0]` е търсеният LCA.
- Сложност: $O(N \log N)$ пресмятане, $O(\log N)$ заявка.

---

## ♾️ Ойлеров път - Алгоритъм на Хирхолцер

Намира Ойлеров цикъл за $O(E)$.
1. Започваме от валиден връх.
2. Вървим по неизползвани ребра, докато забием (върнем се в началото). Това образува цикъл.
3. Ако има върхове по този цикъл с неизползвани ребра, стартираме нов под-цикъл от тях и го "залепяме" (Splice).

---

## 🏁 Заключение

Графите са необятна тема. Binary Lifting е техника, която се използва не само за LCA, но и за всякакви заявки по пътя в дърво (минимум, сума). 0-1 BFS е много чест трик в Grid-базирани задачи.