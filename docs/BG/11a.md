# 📐 Правоъгълници и Геометрични задачи

Правоъгълниците със страни, успоредни на координатните оси, са едни от най-честите обекти в задачите.

## 🏗️ Представяне
Обикновено се пази чрез две точки: долен ляв ъгъл $(x_1, y_1)$ и горен десен ъгъл $(x_2, y_2)$.

```cpp
struct Rect {
    long long x1, y1, x2, y2;
    long long area() { return (x2 - x1) * (y2 - y1); }
};
```

## ✂️ Сечение на два правоъгълника
Сечението на два правоъгълника (ако съществува) също е правоъгълник.
- $x_{left} = \max(r1.x1, r2.x1)$
- $x_{right} = \min(r1.x2, r2.x2)$
- Аналогично за $y$.
- Ако $x_{left} < x_{right}$ и $y_{bottom} < y_{top}$, имаме сечение.

## ➕ Обединение (Area of Union)
Намирането на площта на обединението на $N$ правоъгълника е класическа задача.
- **Метод на замитащата права (Sweep-line)**: Движим вертикална права и следим кои сегменти от нея са покрити. Сложност $O(N \log N)$. (Виж Тема 27).

---

## 🏁 Заключение
Работата с правоъгълници изисква внимателно боравене с неравенства. Винаги проверявайте дали правоъгълникът не е "изроден" (с нулева площ).
