# 🎲 Случайни числа и Рандомизация

Случайните числа са незаменими при симулации, хеширане и решаване на задачи чрез рандомизирани алгоритми.

## ⚠️ Проблемът с `rand()`
Стандартната функция `rand()` в C++ често има малък диапазон (до 32767) и лошо разпределение. **Не се препоръчва за състезания.**

## 🚀 Модерният начин: `<random>`
Библиотеката предоставя висококачествени генератори като **Mersenne Twister (mt19937)**.

### Инициализация
Винаги използвайте текущото време за "seed", за да получавате различни числа при всяко стартиране.
```cpp
#include <random>
#include <chrono>

// Глобален генератор
mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());

// Функция за число в интервала [l, r]
int getRandom(int l, int r) {
    uniform_int_distribution<int> dist(l, r);
    return dist(rng);
}
```

## 📊 Приложения
1. **Рандомизиран QuickSort**: Избор на произволен Pivot предотвратява $O(N^2)$.
2. **Monte Carlo методи**: Пресмятане на площи или вероятности чрез масово генериране на точки.
3. **Хеширане**: Генериране на случайна база за полиномни хешове (предотвратява колизии).

---

## 🏁 Заключение
Винаги предпочитайте `mt19937` пред `rand()`. Качествената случайност е разликата между работещ алгоритъм и такъв, който забива на специфични тестове.
