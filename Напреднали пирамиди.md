# 🧱 Сложни структури - Пирамиди (Heaps)

## 🧱 Биномна пирамида (Binomial Heap)

Биномната пирамида е колекция от **биномни дървета**. Тя поддържа същите операции като стандартната пирамида, но е по-ефективна при **сливане (Merge)** на две пирамиди.

### Свойства на биномното дърво $B_k$:
- Има точно $2^k$ възела.
- Височината му е $k$.
- Може да се получи чрез свързване на две дървета $B_{k-1}$ към корена.

**Сложности**:
- Вмъкване: $O(\log n)$
- Сливане: $O(\log n)$
- Премахване на минимум: $O(\log n)$

---

## 🧬 Фибоначиева пирамида (Fibonacci Heap)

Това е изключително ефективна структура, използвана за оптимизиране на алгоритми върху графи (напр. Алгоритъм на Дейкстра).

**Сложности (Амортизирани)**:
- Вмъкване: **$O(1)$**
- Намаляване на ключ (Decrease-key): **$O(1)$**
- Сливане: **$O(1)$**
- Премахване на минимум: $O(\log n)$

Благодарение на $O(1)$ амортизирана сложност за `decrease-key`, алгоритъмът на Дейкстра може да работи за $O(E + V \log V)$.

---

## ⚡ Сравнение на пирамидите

| Операция | Двоична | Биномна | Фибоначиева |
| :--- | :---: | :---: | :---: |
| **Insert** | $O(\log n)$ | $O(\log n)$ | $O(1)$ |
| **Find Min** | $O(1)$ | $O(\log n)$ | $O(1)$ |
| **Delete Min** | $O(\log n)$ | $O(\log n)$ | $O(\log n)$ |
| **Merge** | $O(n)$ | $O(\log n)$ | $O(1)$ |
| **Decrease Key** | $O(\log n)$ | $O(\log n)$ | $O(1)$ |

---

## 🏁 Заключение

Макар че в повечето състезания стандартната `std::priority_queue` (Двоична пирамида) е достатъчна, разбирането на Биномните и Фибоначиевите пирамиди е ключово за работа с големи графи и оптимизиране на сливанията на структури от данни. 🚀
