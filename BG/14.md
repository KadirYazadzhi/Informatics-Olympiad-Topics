# 🔁 Понятие рекурсия

Рекурсията е фундаментална концепция в програмирането, при която функция извиква сама себе си, докато не бъде изпълнено определено условие за край ("базов случай"). Тя е мощен инструмент за решаване на сложни задачи, като разделя проблема на по-малки подобни задачи.


## 📋 Съдържание
1. **Какво е рекурсия?**
    - Основни елементи на рекурсията
    - Примери за рекурсия
2. **🔍 Търсене с връщане (Backtracking)**
    - Какво е търсене с връщане?
    - Пример за решаване на задачи чрез backtracking
3. **🧮 Търсене в сортиран масив**
    - Линейно търсене в сортиран масив
    - Двоично търсене
4. **🛠️ Двоично търсене**
    - Итеративна реализация
    - Рекурсивна реализация


## 🔁 Какво е рекурсия?

Рекурсията се базира на три ключови компонента:
1. **Базов случай**: Условие, което спира рекурсивните извиквания.
2. **Рекурсивен случай**: Част от кода, която извиква същата функция с модифициран аргумент.
3. **Декомпозиция**: Разделяне на задачата на по-малки подзадачи.

### Пример: Факториел на число
```cpp
#include <iostream>

int factorial(int n) {
    if (n == 0) return 1; // Базов случай
    return n * factorial(n - 1); // Рекурсивен случай
}

int main() {
    int num = 5;
    std::cout << "Факториел на " << num << " е: " << factorial(num) << std::endl;
    return 0;
}
```

### Пример: Фибоначи числа
```cpp
#include <iostream>

int fibonacci(int n) {
    if (n <= 1) return n; // Базов случай
    return fibonacci(n - 1) + fibonacci(n - 2); // Рекурсивен случай
}

int main() {
    int n = 7;
    std::cout << "Фибоначи от " << n << " е: " << fibonacci(n) << std::endl;
    return 0;
}
```


## 🔍 Търсене с връщане (Backtracking)

Търсенето с връщане е алгоритмичен метод, който опитва всички възможности за решаване на даден проблем. Ако текущото решение не е успешно, алгоритъмът се връща назад и опитва друг път.

### Основни елементи на Backtracking:
1. Генериране на всички възможни опции.
2. Проверка дали текущото решение е валидно.
3. Рекурсивно продължаване към следващата стъпка.
4. Връщане, ако се достигне задънена улица.

### Пример: Решение на задача с осемте царици
```cpp
#include <iostream>
#include <vector>

bool isSafe(const std::vector<std::vector<int>>& board, int row, int col) {
    for (int i = 0; i < col; i++)
        if (board[row][i]) return false;

    for (int i = row, j = col; i >= 0 && j >= 0; i--, j--)
        if (board[i][j]) return false;

    for (int i = row, j = col; i < board.size() && j >= 0; i++, j--)
        if (board[i][j]) return false;

    return true;
}

bool solveNQueens(std::vector<std::vector<int>>& board, int col) {
    if (col >= board.size()) return true;

    for (int i = 0; i < board.size(); i++) {
        if (isSafe(board, i, col)) {
            board[i][col] = 1;

            if (solveNQueens(board, col + 1)) return true;

            board[i][col] = 0; // Backtrack
        }
    }

    return false;
}

int main() {
    int n = 8;
    std::vector<std::vector<int>> board(n, std::vector<int>(n, 0));

    if (solveNQueens(board, 0)) {
        for (const auto& row : board) {
            for (int cell : row) std::cout << cell << " ";
            std::cout << std::endl;
        }
    } else {
        std::cout << "Няма решение" << std::endl;
    }

    return 0;
}
```


## 🧮 Търсене в сортиран масив

### Линейно търсене в сортиран масив
```cpp
#include <iostream>
#include <vector>

int linearSearch(const std::vector<int>& arr, int target) {
    for (int i = 0; i < arr.size(); i++) {
        if (arr[i] == target) return i;
    }
    return -1;
}

int main() {
    std::vector<int> arr = {1, 3, 5, 7, 9};
    int target = 5;
    int index = linearSearch(arr, target);
    std::cout << "Индекс на " << target << " е: " << index << std::endl;
    return 0;
}
```


## 🛠️ Двоично търсене

Двоичното търсене работи само със сортирани масиви. Разделя масива на две части и търси в подходящата половина, докато не намери елемента или не останат елементи за проверка.

### Итеративна реализация
```cpp
#include <iostream>
#include <vector>

int binarySearch(const std::vector<int>& arr, int target) {
    int left = 0, right = arr.size() - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;

        if (arr[mid] == target) return mid;
        if (arr[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}

int main() {
    std::vector<int> arr = {1, 3, 5, 7, 9};
    int target = 7;
    int index = binarySearch(arr, target);
    std::cout << "Индекс на " << target << " е: " << index << std::endl;
    return 0;
}
```

### Рекурсивна реализация
```cpp
#include <iostream>
#include <vector>

int binarySearchRecursive(const std::vector<int>& arr, int left, int right, int target) {
    if (left > right) return -1;

    int mid = left + (right - left) / 2;

    if (arr[mid] == target) return mid;
    if (arr[mid] < target) return binarySearchRecursive(arr, mid + 1, right, target);

    return binarySearchRecursive(arr, left, mid - 1, target);
}

int main() {
    std::vector<int> arr = {1, 3, 5, 7, 9};
    int target = 9;
    int index = binarySearchRecursive(arr, 0, arr.size() - 1, target);
    std::cout << "Индекс на " << target << " е: " << index << std::endl;
    return 0;
}
```


## 🎯 Заключение
Тези алгоритми и подходи предлагат мощни инструменти за решаване на различни проблеми в програмирането. Разбирането на тяхната логика и ефективна реализация е от ключово значение за писането на оптимизиран код.
