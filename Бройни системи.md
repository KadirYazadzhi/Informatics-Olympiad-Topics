# 🔢 Бройни системи, дълги числа и случайни числа

## 🔢 Бройни системи
Бройната система е метод за представяне на числата с помощта на символи. Най-често използваната система е десетичната (десяткова) система, която използва десет цифри (0-9). Освен нея, често срещани са:

- **Двоична (бинарна) система** – използва само 0 и 1, широко използвана в компютърните технологии.
- **Осмична система** – използва цифрите 0-7, използвана най-вече в стари компютърни системи и някои изчислителни устройства.
- **Шестнадесетична (хексадецимална) система** – използва цифрите 0-9 и буквите A-F, намира приложение в програмирането, цветовите кодове и машинния език.

## Бройни системи
# История и значение
Различните бройни системи са възникнали в зависимост от нуждите на цивилизациите. Например, вавилонците са използвали шейсетична бройна система, докато римляните са използвали своята система от римски цифри. В днешно време, двоичната система е основата на цифровите технологии, което прави разбирането ѝ критично важно за програмистите и инженерите.

### Конвертиране между бройни системи
Конвертирането между различни бройни системи е основна операция. Например, при конвертиране от десетична в двоична система числото се дели на 2, а остатъците се записват в обратен ред. От двоична в десетична система се използва сбор от степените на 2 за всяка цифра. От десетична в шестнадесетична се извършва деление на 16.

Пример за конвертиране на числото `45` в двоична система:
```
45 / 2 = 22 остатък 1
22 / 2 = 11 остатък 0
11 / 2 = 5 остатък 1
5 / 2 = 2 остатък 1
2 / 2 = 1 остатък 0
1 / 2 = 0 остатък 1
```
Резултат: `101101`

Съществуват и алгоритми за директно конвертиране между различни системи, като методът на Хорнер, който позволява бързо пресмятане без необходимост от междинна десетична стойност.

## 🧮 Дълги числа
Дългите числа са числа, които надвишават капацитета на стандартните типове данни като `int` и `long`. Те се обработват като масиви или низове, за да се извършват аритметични операции. 

### Представяне на дългите числа
Понеже стандартните типове имат ограничен размер (например 32-битов или 64-битов `int`), дългите числа обикновено се съхраняват като масиви от цифри или низове. За тях се прилагат специални алгоритми за обработка.

### Аритметични операции
При събиране на две дълги числа се работи с тях като със символни низове, обработвайки ги цифра по цифра отдясно наляво. Подобно е и при умножението, където се използва поцифрено умножение с пренасяне.

Пример за събиране на дълги числа:
```
12345678901234567890
+ 98765432109876543210
----------------------
 111111111011111111100
```

Методът за умножение на дълги числа наподобява стандартния метод за умножение на ръка, като се извършват операции за всяка цифра и се събират междинните резултати. По-оптимизиран вариант е използването на алгоритъма на Карацуба, който разделя числата на части и намалява броя на необходимите операции.

## 🎲 Случайни числа
Случайните числа са числа, които не следват определен шаблон и са генерирани от алгоритъм или физически процес. В компютърните науки те се генерират чрез:
- **Псевдослучайни генератори** – алгоритми, които симулират случайност (например линеен конгруентен генератор).
- **Истински случайни числа** – базирани на физически явления като радиоактивен разпад, шум в електронни схеми и атмосферни флуктуации.

### Генериране на случайни числа
В различни програмни езици могат да се използват вградени функции за генериране на случайни числа.

Пример в **Python**:
```python
import random
print(random.randint(1, 100))
```

Пример в **C++**:
```cpp
#include <iostream>
#include <cstdlib>
#include <ctime>
int main() {
    std::srand(std::time(0));
    std::cout << rand() % 100 + 1 << std::endl;
    return 0;
}
```

### Разпределения на случайни числа
Случайните числа могат да следват различни разпределения като:
- **Равномерно разпределение** – всички числа в даден интервал имат еднаква вероятност.
- **Нормално разпределение** – често срещано в природата и статистиката, характеризира се с концентрация около средната стойност.
- **Експоненциално разпределение** – използва се за моделиране на времеви интервали между независими събития.

В реалния свят случайните числа намират приложение в криптографията, симулациите, статистическите анализи и игрите. Псевдослучайните генератори като Mersenne Twister осигуряват добра производителност за повечето приложения, докато криптографските генератори предлагат по-висока сигурност срещу предсказуемост.
