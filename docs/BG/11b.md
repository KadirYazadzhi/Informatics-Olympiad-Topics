# 🌌 Квадратни мрежи, Лабиринти и Области

Мрежите (Grids) са двумерни масиви, които моделират физическо пространство.

## 🗺️ Представяне и Движение
Обикновено използваме `int grid[N][M]`. За движение в 4 посоки (нагоре, надолу, ляво, дясно) използваме масиви с посоки:
```cpp
int dx[] = {-1, 1, 0, 0};
int dy[] = {0, 0, -1, 1};

for (int i = 0; i < 4; i++) {
    int nx = x + dx[i];
    int ny = y + dy[i];
    if (isValid(nx, ny)) { /* ход */ }
}
```

## 🏃‍♂️ Търсене на път

### 1. BFS (Breadth-First Search)
Намира **най-краткия път** в мрежа (ако няма тегла).
- Използва се опашка (`std::queue`).
- Пазим масив `dist[N][M]` за изминатото разстояние.

### 2. DFS (Depth-First Search)
Използва се за проверка на достижимост и намиране на свързани области (Flood Fill).

## 🌊 Flood Fill Алгоритъм
Запълване на област с определен цвят.
- **Пример**: Колко клетки има в "езерото", в което сме попаднали?
```cpp
void floodFill(int x, int y) {
    if (!isValid(x, y) || visited[x][y]) return;
    visited[x][y] = true;
    count++;
    for (int i = 0; i < 4; i++) floodFill(x + dx[i], y + dy[i]);
}
```

---

## 🏁 Заключение
Мрежите са в основата на много алгоритми върху графи. Овладяването на BFS/DFS върху мрежа е задължително за всеки начинаещ програмист.
