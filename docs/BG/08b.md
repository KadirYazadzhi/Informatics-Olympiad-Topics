# 🧵 Прости числа и Решето на Ератостен

Простите числа са "атомите" на числата. Всяко естествено число $>1$ може да се разложи по уникален начин на произведение от прости числа.

## 🔎 Проверка за просто число (Прималност)
Най-бързият начин да проверим дали $N$ е просто, е да пробваме деление на всички числа до $\sqrt{N}$.
```cpp
bool isPrime(long long n) {
    if (n < 2) return false;
    for (long long i = 2; i * i <= n; i++) {
        if (n % i == 0) return false;
    }
    return true;
}
```
Сложност: $O(\sqrt{N})$.

---

## 🧶 Решето на Ератостен (Sieve of Eratosthenes)
Използва се за намиране на **всички** прости числа в диапазона $[1, N]$.

### Алгоритъм:
1. Създаваме масив `isPrime` с размер $N+1$, запълнен с `true`.
2. Започваме от 2. Ако е маркирано като просто, зачеркваме всички негови кратни ($4, 6, 8 \dots$).
3. Продължаваме до $\sqrt{N}$.

```cpp
vector<int> primes;
bool is_prime[MAXN];
void sieve(int n) {
    fill(is_prime, is_prime + n + 1, true);
    is_prime[0] = is_prime[1] = false;
    for (int p = 2; p * p <= n; p++) {
        if (is_prime[p]) {
            for (int i = p * p; i <= n; i += p)
                is_prime[i] = false;
        }
    }
}
```
**Сложност**: $O(N \log \log N)$ – практически линейно време.

---

## 🔢 Разлагане на прости множители
Всяко число $N$ може да се разложи чрез последователно деление на простите числа до $\sqrt{N}$.
```cpp
void factorize(long long n) {
    for (long long i = 2; i * i <= n; i++) {
        while (n % i == 0) {
            cout << i << " ";
            n /= i;
        }
    }
    if (n > 1) cout << n;
}
```

## 🏁 Заключение
Решетото е изключително мощен инструмент. Ако задачата изисква работа с много числа до $10^6$ или $10^7$, решетото е единственият начин да се справите в ограничението по време.
