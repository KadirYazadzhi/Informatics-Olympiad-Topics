# 🌐 Напреднали графи: Дълбока теория

## 🤝 Двупартитни съчетания (Bipartite Matching)

### Теорема на Хол (Hall's Marriage Theorem)
Двупартитен граф с части $X$ и $Y$ има пълно съчетание, покриващо $X$, тогава и само тогава, когато за всяко подмножество $S \subseteq X$, броят на съседите му $|N(S)| \ge |S|$.

### Алгоритъм на Хопкрофт-Карп (Hopcroft-Karp)
Намира максимално съчетание за $O(E \sqrt{V})$.
- Работи на фази. Във всяка фаза намира **всички** най-къси увеличаващи пътища едновременно чрез BFS и ги използва чрез DFS.

---

## 🛤️ Силна свързаност (SCC) - 2-SAT

Задачата **2-Satisfiability (2-SAT)**:
Даден е логически израз в конюнктивна нормална форма, където всяка скоба има 2 литерала: $(x_1 \lor \neg x_2) \land (\neg x_1 \lor x_3) \dots$
Може ли да се присвоят стойности True/False, така че изразът да е True?

**Решение чрез SCC**:
1. Построяваме импликационен граф. За $(A \lor B)$ добавяме ребра $\neg A \to B$ и $\neg B \to A$.
2. Намираме SCC.
3. Ако за някоя променлива $x$, $x$ и $\neg x$ са в един и същ компонент $\to$ **Няма решение**.
4. Иначе, решение съществува. Ако $topological(SCC(x)) > topological(SCC(\neg x))$, то $x = \text{True}$.

---

## 🌳 Доминатори в граф (Dominator Tree)

В oriented graph (Flow graph) с начален връх $S$, връх $U$ доминира $V$, ако всеки път от $S$ до $V$ минава през $U$.
- Доминаторното дърво показва йерархията на доминиране.
- Използва се за оптимизация на компилатори и анализ на паметта.
- Алгоритъм на Ленгауер-Таржан: $O(E \alpha(V))$.

---

## 🏁 Заключение

2-SAT е класическа задача, която се свежда директно до SCC. Хопкрофт-Карп е рядко нужен (Кун/Flow обикновено стигат), но е единственото решение за $V, E \approx 10^5$.