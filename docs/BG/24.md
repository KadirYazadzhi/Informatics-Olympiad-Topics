# 🕸️ Алгоритми върху Графи: Основи и Оптимизации

Графите са мощна структура за моделиране на връзки между обекти (градове, компютри, хора). Тук разглеждаме най-важните алгоритми за обхождане и намиране на най-кратък път.

---

## 1. Обхождане на Граф

### 1.1. DFS (Обхождане в дълбочина)
Използва стек (или рекурсия). Върви колкото се може по-навътре, преди да се върне.
*   **Сложност**: $O(V + E)$.
*   **Приложение**: Намиране на компоненти, проверка за цикли, артикулационни точки.

### 1.2. BFS (Обхождане в ширина)
Използва опашка (`std::queue`). Посещава върховете слой по слой.
*   **Сложност**: $O(V + E)$.
*   **Приложение**: Най-кратък път в непретеглен граф.

---

## 2. Най-кратък път (Shortest Path)

### 2.1. Алгоритъм на Дейкстра (Dijkstra)
За графи с **неотрицателни** тегла на ребрата. Използва приоритетна опашка.
*   **Сложност**: $O(E \log V)$.

```cpp
void dijkstra(int start) {
    dist.assign(n, INF);
    dist[start] = 0;
    priority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<pair<long long, int>>> pq;
    pq.push({0, start});

    while (!pq.empty()) {
        auto [d, u] = pq.top(); pq.pop();
        if (d > dist[u]) continue;
        for (auto [v, w] : adj[u]) {
            if (dist[u] + w < dist[v]) {
                dist[v] = dist[u] + w;
                pq.push({dist[v], v});
            }
        }
    }
}
```

### 2.2. Алгоритъм на Белман-Форд (Bellman-Ford)
Работи и с **отрицателни** тегла. Може да открива отрицателни цикли.
*   **Сложност**: $O(V \cdot E)$.

### 2.3. Алгоритъм на Флойд-Уоршал (Floyd-Warshall)
Намира разстоянието между **всички двойки** върхове.
*   **Сложност**: $O(V^3)$.

```cpp
for (int k = 0; k < n; k++)
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]);
```

---

## 3. Минимално Покриващо Дърво (MST)

### 3.1. Алгоритъм на Крускал (Kruskal)
Сортира ребрата по тегло и ги добавя едно по едно, ако не образуват цикъл (използва DSU).
*   **Сложност**: $O(E \log E)$.

### 3.2. Алгоритъм на Прим (Prim)
Започва от един връх и добавя най-евтиното ребро, свързващо дървото с нов връх.
*   **Сложност**: $O(E \log V)$ с приоритетна опашка.

---

## 4. Топологично Сортиране
Подреждане на върховете на DAG така, че ако има ребро $u \to v$, $u$ е преди $v$. Използва се DFS или Алгоритъм на Кан (Kahn).

---

## 5. Задачи за упражнение
1.  **CSES Shortest Routes I & II**: Dijkstra и Floyd-Warshall.
2.  **CSES Road Reparation**: MST.
3.  **Codeforces 20C**: Dijkstra с възстановяване на пътя.
4.  **UVa 558**: Wormholes (Bellman-Ford).

## 🏁 Заключение
Графовите алгоритми са основата на много състезателни задачи. Изборът на правилен алгоритъм зависи от типа на графа (насочен/ненасочен), теглата (положителни/отрицателни) и броя върхове.
