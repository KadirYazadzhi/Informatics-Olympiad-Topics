# ➗ Делимост на числа и Алгоритъм на Евклид

Теорията на числата е в основата на съвременната криптография и алгоритмична оптимизация.

## 📖 Основни понятия
- **Делимост**: Числото $a$ се дели на $b$ ($b|a$), ако остатъкът $a \pmod b = 0$.
- **Прости числа**: Числа, които имат само два делителя (1 и себе си).
- **Взаимно прости**: Числа, чийто най-голям общ делител е 1.

## 🔢 Алгоритъм на Евклид (НОД)
Ефективен начин за намиране на най-големия общ делител на две числа.

### Итеративна реализация:
```cpp
int gcd(int a, int b) {
    while (b != 0) {
        a %= b;
        swap(a, b);
    }
    return a;
}
```
**Сложност**: $O(\log(\min(a, b)))$. Това е изключително бързо дори за 100-цифрени числа.

## 📏 Най-малко общо кратно (НОК)
Изчислява се чрез формулата:
$$\text{lcm}(a, b) = \frac{|a \cdot b|}{\text{gcd}(a, b)}$$
*Внимание: Първо делим, после умножаваме, за да избегнем препълване (overflow).*

## 🛡️ Свойства на остатъците
- $(a + b) \pmod m = (a \pmod m + b \pmod m) \pmod m$
- $(a \cdot b) \pmod m = (a \pmod m \cdot b \pmod m) \pmod m$
Това позволява работа с огромни резултати чрез постоянно взимане на остатък.

---

## 🏁 Заключение
Разбирането на НОД и свойствата на делението е критично за задачи, включващи цикли, периодичност и дробни изчисления.