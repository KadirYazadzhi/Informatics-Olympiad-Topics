# 🧮 Пресмятане с обикновени дроби

Работата с дроби изисква прецизност и избягване на работа с числа с плаваща запетая (`double`), за да се запази точността.

## 🏗️ Представяне на дроб
Винаги съхранявайте дробите като двойка цели числа: числител ($num$) и знаменател ($den$).

```cpp
struct Fraction {
    long long num, den;
    
    void simplify() {
        long long common = gcd(abs(num), abs(den));
        num /= common;
        den /= common;
        if (den < 0) { num = -num; den = -den; }
    }
};
```

## ➕ Операции с дроби

### 1. Събиране и Изваждане
Трябва да приведем под общ знаменател. Най-добре е да ползваме НОК на знаменателите или просто произведението им.
$$\frac{a}{b} + \frac{c}{d} = \frac{a \cdot d + b \cdot c}{b \cdot d}$$

### 2. Умножение и Деление
Умножаваме съответно числителите и знаменателите.
$$\frac{a}{b} \cdot \frac{c}{d} = \frac{a \cdot c}{b \cdot d}, \quad \frac{a}{b} \div \frac{c}{d} = \frac{a \cdot d}{b \cdot c}$$

## ⚠️ Важни правила
1. **Винаги съкращавайте**: След всяка операция викайте `simplify()`, за да предотвратите препълване на променливите.
2. **Нулев знаменател**: Винаги проверявайте дали знаменателят не става 0.
3. **Сравнение**: Вместо `a/b == c/d`, използвайте крос-умножение: `a * d == b * c`.

---

## 🏁 Заключение
Използването на структури за дроби гарантира абсолютна математическа точност, което е жизненоважно в геометрични и логически задачи.
