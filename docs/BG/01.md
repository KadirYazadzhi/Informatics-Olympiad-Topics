# 🖥️ Среда за програмиране на C++  

## 🚀 Въведение  
C++ е един от най-мощните и широко използвани езици за програмиране, който комбинира високопроизводителност с възможности за обектно-ориентирано програмиране. Използва се в различни области като:  

- Разработка на софтуер  
- Гейминг индустрията  
- Системно програмиране  
- Вградени системи  

В този материал ще разгледаме как да подготвим среда за програмиране, основни типове данни, както и как да използваме вход и изход в C++.


## 🛠️ Подготовка на средата за програмиране  

1. **Инсталиране на компилатор**  
   Компилаторът е инструмент, който превежда вашия C++ код в изпълним файл. Най-често използваните компилатори са:  
   - 🖥️ **GCC (GNU Compiler Collection)** - популярен избор за Linux.  
   - 🖥️ **Clang** - модерен компилатор, използван често с macOS.  
   - 🖥️ **MSVC (Microsoft Visual C++)** - част от Visual Studio.  

   **Пример за инсталиране на GCC на Linux:**  
   ```bash
   sudo apt update
   sudo apt install build-essential
   ```

# 🖥️ Избор на интегрирана среда за разработка (IDE)

IDE улеснява писането, компилирането и тестването на кода. Най-популярните избори за C++ включват:

- 🔵 **Visual Studio** (Windows)  
- 🟢 **Code::Blocks** (Windows/Linux/Mac)  
- 🟣 **CLion** (всички платформи, платена)  
- 🖋️ **Visual Studio Code** (изисква добавяне на разширения за C++).  


# 🚀 Създаване и изпълнение на първа програма

След като имате компилатор и среда за разработка:

1. Създайте файл с разширение `.cpp` (напр. `main.cpp`).  
2. Напишете следния код:  

   ```cpp
   #include <iostream>
   using namespace std;

   int main() {
       cout << "Здравей, свят!" << endl;
       return 0;
   }
   ```

3. Компилирайте програмата с командата:

   ```
   g++ main.cpp -o main
   ```

3. Стартирайте изпълнимия файл:

   ```
   ./main
   ```

# 🔢 Прости типове данни в C++

В C++ има разнообразие от типове данни, които можете да използвате за съхранение и обработка на информация.

<table>
  <thead>
    <tr>
      <th>Тип данни</th>
      <th>Описание</th>
      <th>Размер</th>
      <th>Диапазон</th>
      <th>Пример</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>int</code></td>
      <td>Цяло число</td>
      <td>4 байта</td>
      <td>-2,147,483,648 до 2,147,483,647</td>
      <td><code>int a = 42;</code></td>
    </tr>
    <tr>
      <td><code>float</code></td>
      <td>Число с плаваща запетая</td>
      <td>4 байта</td>
      <td>~±3.4×10⁻³⁸ до ±3.4×10³⁸</td>
      <td><code>float b = 3.14;</code></td>
    </tr>
    <tr>
      <td><code>double</code></td>
      <td>Двойна точност на плаваща запетая</td>
      <td>8 байта</td>
      <td>~±1.7×10⁻³⁰⁸ до ±1.7×10³⁰⁸</td>
      <td><code>double c = 2.71828;</code></td>
    </tr>
    <tr>
      <td><code>char</code></td>
      <td>Символ (ASCII)</td>
      <td>1 байт</td>
      <td>0 до 255</td>
      <td><code>char d = 'A';</code></td>
    </tr>
    <tr>
      <td><code>bool</code></td>
      <td>Булев тип (истина/лъжа)</td>
      <td>1 байт</td>
      <td><code>true</code> или <code>false</code></td>
      <td><code>bool e = true;</code></td>
    </tr>
  </tbody>
</table>


### 🔑 Пример за работа с типове данни
```
#include <iostream>
using namespace std;

int main() {
    int number = 10;
    float pi = 3.14;
    char letter = 'A';
    bool isTrue = true;

    cout << "Число: " << number << endl;
    cout << "Пи: " << pi << endl;
    cout << "Буква: " << letter << endl;
    cout << "Булева стойност: " << isTrue << endl;

    return 0;
}
```

## 🔤 Вход и изход на програма

### 📥 Вход от потребителя

C++ използва `cin` за четене на данни от клавиатурата.

**Четене на едно число:**
```cpp
#include <iostream>
using namespace std;

int main() {
    int age;
    cout << "Въведете вашата възраст: ";
    cin >> age;
    cout << "Вие сте на " << age << " години." << endl;
    return 0;
}
```

**Четене на множество променливи:**
```cpp
int a, b;
cout << "Въведете две числа: ";
cin >> a >> b;
cout << "Сумата е: " << (a + b) << endl;
```

**Четене на низ с интервали:**
```cpp
string name;
cout << "Въведете вашето име: ";
getline(cin, name);  // Чете цял ред
cout << "Здравей, " << name << "!" << endl;
```

### 📤 Изход към конзолата

За изписване на резултати използваме `cout`.

**Основен пример:**
```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Добре дошли в света на програмирането!" << endl;
    return 0;
}
```

**Форматиране на изход:**
```cpp
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double pi = 3.14159265359;
    
    // Задаване на прецизност
    cout << fixed << setprecision(2);
    cout << "Pi е приблизително: " << pi << endl;  // 3.14
    
    // Задаване на ширина
    cout << setw(10) << "Име" << setw(10) << "Възраст" << endl;
    cout << setw(10) << "Иван" << setw(10) << 25 << endl;
    
    return 0;
}
```

### 💡 Бележка за форматиране

- `endl` се използва за нов ред и изчистване на буфера
- `\n` е по-бърза алтернатива (не изчиства буфера)
- `flush` изчиства буфера без да добавя нов ред

```cpp
cout << "Текст" << endl;   // Нов ред + flush
cout << "Текст\n";         // Само нов ред
cout << "Текст" << flush;  // Само flush
```

## 📁 Работа с файлове

### Четене от файл
```cpp
#include <fstream>
#include <iostream>
using namespace std;

int main() {
    ifstream input("input.txt");
    
    if (!input.is_open()) {
        cout << "Грешка при отваряне на файла!" << endl;
        return 1;
    }
    
    int number;
    while (input >> number) {
        cout << number << " ";
    }
    
    input.close();
    return 0;
}
```

### Писане във файл
```cpp
#include <fstream>
using namespace std;

int main() {
    ofstream output("output.txt");
    
    output << "Резултат: " << 42 << endl;
    output << "Успешно записано!" << endl;
    
    output.close();
    return 0;
}
```

## 🔧 Модификатори и типове данни

### Допълнителни типове
```cpp
// Големи числа
long long bigNum = 1000000000000LL;

// Беззнакови типове
unsigned int positiveOnly = 4000000000U;

// Къси и дълги
short smallNum = 32767;
long int longNum = 2147483647L;

// Размери на типовете
cout << "int: " << sizeof(int) << " байта" << endl;
cout << "double: " << sizeof(double) << " байта" << endl;
```

### Константи
```cpp
const double PI = 3.14159;
const int MAX_SIZE = 100;

// C++11 constexpr за compile-time константи
constexpr int DAYS_IN_WEEK = 7;
```

### Автоматично определяне на тип (C++11)
```cpp
auto x = 5;        // int
auto y = 3.14;     // double
auto z = "text";   // const char*
auto s = string("hello");  // string
```

## 🎯 Практически примери

### Калкулатор
```cpp
#include <iostream>
using namespace std;

int main() {
    double a, b;
    char op;
    
    cout << "Въведете израз (напр. 5 + 3): ";
    cin >> a >> op >> b;
    
    switch(op) {
        case '+': cout << "Резултат: " << (a + b) << endl; break;
        case '-': cout << "Резултат: " << (a - b) << endl; break;
        case '*': cout << "Резултат: " << (a * b) << endl; break;
        case '/':
            if (b != 0)
                cout << "Резултат: " << (a / b) << endl;
            else
                cout << "Грешка: Деление на нула!" << endl;
            break;
        default:
            cout << "Невалидна операция!" << endl;
    }
    
    return 0;
}
```

### Температурен конвертор
```cpp
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double celsius;
    cout << "Въведете температура в Целзий: ";
    cin >> celsius;
    
    double fahrenheit = celsius * 9.0 / 5.0 + 32;
    double kelvin = celsius + 273.15;
    
    cout << fixed << setprecision(2);
    cout << celsius << "°C = " << fahrenheit << "°F" << endl;
    cout << celsius << "°C = " << kelvin << "K" << endl;
    
    return 0;
}
```

### Таблица за умножение
```cpp
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int n;
    cout << "Въведете число: ";
    cin >> n;
    
    cout << "Таблица за умножение на " << n << ":" << endl;
    for (int i = 1; i <= 10; i++) {
        cout << setw(2) << n << " x " << setw(2) << i 
             << " = " << setw(3) << (n * i) << endl;
    }
    
    return 0;
}
```

## ✅ Основни концепции

### Програмен поток
- Кодът се изпълнява отгоре надолу
- Условни оператори (`if`, `else`, `switch`) променят потока
- Цикли (`for`, `while`, `do-while`) повтарят изпълнението

### Оператори
```cpp
// Аритметични
int sum = 5 + 3;      // 8
int diff = 5 - 3;     // 2
int prod = 5 * 3;     // 15
int quot = 5 / 3;     // 1 (целочислено деление)
int mod = 5 % 3;      // 2 (остатък)

// Инкремент/Декремент
int x = 5;
x++;  // x = 6 (post-increment)
++x;  // x = 7 (pre-increment)

// Съкратени оператори
x += 5;  // x = x + 5
x *= 2;  // x = x * 2
```

### Коментари в C++

**Едноредови коментари:**
```cpp
// Това е едноредов коментар
int x = 5;  // Коментар след код
```

**Многоредови коментари:**
```cpp
/* 
   Това е многореден коментар
   Може да обхваща множество редове
   Полезен за документация
*/
```

### Променливи и области на видимост
```cpp
#include <iostream>
using namespace std;

int globalVar = 10;  // Глобална променлива

int main() {
    int localVar = 5;  // Локална променлива
    
    {
        int blockVar = 3;  // Променлива във вътрешен блок
        cout << blockVar << endl;  // OK
    }
    
    // cout << blockVar << endl;  // ГРЕШКА! blockVar не е видима
    cout << localVar << endl;     // OK
    cout << globalVar << endl;    // OK
    
    return 0;
}
```

## 🛡️ Обработка на грешки

### Проверка на входа
```cpp
#include <iostream>
#include <limits>
using namespace std;

int main() {
    int num;
    cout << "Въведете число: ";
    
    while (!(cin >> num)) {
        cout << "Невалиден вход! Опитайте отново: ";
        cin.clear();  // Изчиства флаговете за грешка
        cin.ignore(numeric_limits<streamsize>::max(), '\n');  // Игнорира лошия вход
    }
    
    cout << "Въведохте: " << num << endl;
    return 0;
}
```

## 💡 Съвети за ефективност

1. **Използвайте подходящи типове**: `int` за цели числа, `double` за дроби
2. **Избягвайте излишни копия**: Използвайте референции за големи обекти
3. **Оптимизирайте I/O**: За състезателно програмиране използвайте:
   ```cpp
   ios_base::sync_with_stdio(false);
   cin.tie(NULL);
   ```
4. **Преизползвайте променливи**: Не създавайте нови променливи ако не е нужно

## 🏁 Заключение

C++ е мощен език с богати възможности за програмиране. Правилното разбиране на типовете данни, входа/изхода и основните концепции е критично за успеха в програмирането. С практика и експериментиране ще развиете увереност в използването на C++ за решаване на разнообразни задачи. Успех! 🚀
