# 🔄 Динамично програмиране (Dynamic Programming)

## 🎯 Въведение

Динамичното програмиране (ДП) е метод за решаване на сложни задачи чрез разбиването им на по-малки подзадачи. То се използва, когато подзадачите се припокриват (overlapping subproblems) и имат оптимална структура (optimal substructure).

Два основни подхода:
1. **Top-down (Memoization)**: Рекурсия с кеширане на резултатите.
2. **Bottom-up (Tabulation)**: Итеративно попълване на таблица.

---

## 📂 Класически задачи

### 1. Едномерно ДП: Числа на Фибоначи / Стълби
За да стигнете до стъпало $i$, можете да дойдете от $i-1$ или $i-2$.
$$dp[i] = dp[i-1] + dp[i-2]$$

### 2. Двумерно ДП: Най-дълъг общ подниз (LCS)
Намиране на дължината на най-дългата поредица, която е подниз и на двата низа $S$ и $T$.
- Ако $S[i] == T[j]$: $dp[i][j] = dp[i-1][j-1] + 1$
- Иначе: $dp[i][j] = \max(dp[i-1][j], dp[i][j-1])$

---

## 📏 Разширени класически задачи

### Най-къс обхващащ низ (Shortest Common Supersequence)
Намиране на най-късия низ, който съдържа и двата низа като поднизове.
Свързано с LCS: $\text{Length}(S) + \text{Length}(T) - \text{LCS}(S, T)$.

### Преобразуване на рекурсивни програми
Много рекурсивни алгоритми с експоненциална сложност могат да се оптимизират до полиномни чрез добавяне на масив за запаметяване (memoization).

---

## 🚀 Специфични техники в ДП

### 1. ДП с битови маски (Bitmask DP)
Използва се, когато състоянието зависи от това кои елементи от малко множество са избрани.
- **Пример**: Задача за търговския пътник (TSP).
- **Сложност**: Обикновено $O(2^n \cdot n^k)$.

### 2. ДП по цифри (Digit DP)
Използва се за преброяване на числа в интервала $[A, B]$, които отговарят на определено свойство (напр. сумата на цифрите им е $K$).
- **Състояние**: `dp(index, current_sum, is_less, is_started)`.

### 3. ДП в дърво (Tree DP)
Решаване на задачи върху графове без цикли.
- **Пример**: Намиране на максимално независимо множество в дърво.
- **Подход**: Обикновено чрез DFS обхождане.

### 4. ДП в ациклични ориентирани графи (DAG)
Тъй като ДП изисква подреденост на подзадачите, всеки ДП модел може да се представи като DAG. Най-дълъг път в DAG се намира лесно чрез ДП.

---

## 🛠️ Пример: Раницата (0/1 Knapsack)
Имаме предмети с тегло $w_i$ и стойност $v_i$. Търсим максимална стойност за капацитет $W$.

```cpp
for (int i = 0; i < n; i++) {
    for (int j = W; j >= w[i]; j--) {
        dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
    }
}
```

---

## 🏁 Заключение

Динамичното програмиране е най-важната тема за напреднали състезатели. Ключът е в правилното дефиниране на **състоянието** и **преходите** между тях. Практиката с класически задачи е най-добрият начин за усвояване на метода. 🚀

```