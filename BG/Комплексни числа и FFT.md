# 📐 Комплексни числа и FFT: Експертно ниво

## 🔢 Number Theoretic Transform (NTT)

FFT работи с комплексни числа (`double`), което води до грешки от закръгляне.
NTT е еквивалентът на FFT за модулна аритметика.
- Вместо комплексни корени на единицата, използваме **примитивни корени** по модул $P$.
- Изисква $P$ да е от вида $c \cdot 2^k + 1$ (напр. $998244353 = 119 \cdot 2^{23} + 1$).
- Примитивен корен за $998244353$ е $g=3$.

**Предимство**: Работи с цели числа, няма грешки, резултатът е точен по модул $P$.

## 🏗️ Fast Walsh-Hadamard Transform (FWHT)

Алгоритъм за бързо умножение на полиноми, но вместо $x^i \cdot x^j = x^{i+j}$, операцията в степента е $i \oplus j$ (XOR), $i \& j$ (AND) или $i | j$ (OR).
- Използва се за задачи: "Дадени са масиви $A$ и $B$. Намерете $C[k] = \sum_{i \oplus j = k} A[i] B[j]$".
- Сложност: $O(N \log N)$.

---

## 🏁 Заключение

Ако задачата е "конволюция" (сума от произведения с фиксиран сбор/XOR на индексите), отговорът е FFT/NTT или FWHT. Това е стандартна техника за преброяване на комбинации.