# ➗ Делимост на числа, алгоритъм на Евклид и негови приложения


## 📚 Делимост на числа

Делимостта е фундаментална концепция в теорията на числата. Числото `a` се дели на числото `b` (където `b ≠ 0`), ако съществува цяло число `k`, такова че `a = b * k`. В този случай казваме, че `b` е делител на `a`, а `a` е кратно на `b`.

**Пример:**
- Числото `15` се дели на `3`, защото `15 = 3 * 5`.

### Проверка за делимост
Често срещаните правила за делимост включват:
- **Делимост на 2:** Числото завършва на четна цифра (0, 2, 4, 6, 8).
- **Делимост на 3:** Сумата от цифрите на числото се дели на 3.
- **Делимост на 5:** Числото завършва на 0 или 5.

**Примерен код:**
```cpp
#include <iostream>
using namespace std;

int main() {
    int num;
    cout << "Въведете число: ";
    cin >> num;

    if (num % 2 == 0) {
        cout << num << " е четно." << endl;
    }
    if (num % 3 == 0) {
        cout << num << " се дели на 3." << endl;
    }
    if (num % 5 == 0) {
        cout << num << " се дели на 5." << endl;
    }

    return 0;
}
```

## 🔢 Алгоритъм на Евклид и негови приложения

### Какво е алгоритъмът на Евклид?
Алгоритъмът на Евклид е ефективен метод за намиране на най-големия общ делител (НОД) на две числа. Той се базира на принципа, че НОД на две числа не се променя, ако по-голямото число се замести с разликата между двете числа.

Формулировка:
  - Ако `b = 0`, тогава `НОД(a, b) = a`.
  - В противен случай `НОД(a, b) = НОД(b, a % b)`.

Пример
За намиране на `НОД(48, 18)`:
  1. `48 % 18 = 12`
  2. `18 % 12 = 6`
  3. `12 % 6 = 0`, следователно `НОД(48, 18) = 6`.


Примерен код
```cpp
#include <iostream>
using namespace std;

int gcd(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}

int main() {
    int a, b;
    cout << "Въведете две числа: ";
    cin >> a >> b;

    cout << "НОД(" << a << ", " << b << ") = " << gcd(a, b) << endl;
    return 0;
}
```

## 🧮 Пресмятане с обикновени дроби

Обикновените дроби представляват част от цяло число, изразено чрез числител (горната част) и знаменател (долната част).

### Основни операции

- **Събиране на дроби**
```(a / b) + (c / d) = (a * d + b * c) / (b * d)```

- **Изваждане на дроби**
```(a / b) - (c / d) = (a * d - b * c) / (b * d)```

- **Умножение на дроби**
```(a / b) * (c / d) = (a * c) / (b * d)```

- **Деление на дроби**
```(a / b) ÷ (c / d) = (a * d) / (b * c)```


Примерен код за събиране на дроби
```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c, d;
    cout << "Въведете числител и знаменател за първата дроб: ";
    cin >> a >> b;
    cout << "Въведете числител и знаменател за втората дроб: ";
    cin >> c >> d;

    int numerator = a * d + b * c;
    int denominator = b * d;

    cout << "Резултат: " << numerator << "/" << denominator << endl;
    return 0;
}
```

## 🧵 Прости числа и решето на Ератостен

### Какво е просто число?

Простото число е естествено число, по-голямо от 1, което има точно два делителя: 1 и самото себе си.

### Решето на Ератостен

Решето на Ератостен е ефективен алгоритъм за намиране на всички прости числа до дадено число `n`.

#### Алгоритъм:
1. Създаваме булев масив от 2 до `n`, където всички елементи са маркирани като „истина“ (първоначално всички числа са прости).
2. Започваме от `p = 2` и премахваме всички негови кратни.
3. Преминаваме към следващото число, което не е премахнато.
4. Процесът се повтаря до `n`.


Примерен код:
```cpp
#include <iostream>
#include <vector>
using namespace std;

void sieveOfEratosthenes(int n) {
    vector<bool> isPrime(n + 1, true);
    isPrime[0] = isPrime[1] = false;

    for (int p = 2; p * p <= n; p++) {
        if (isPrime[p]) {
            for (int i = p * p; i <= n; i += p) {
                isPrime[i] = false;
            }
        }
    }

    cout << "Простите числа до " << n << " са: ";
    for (int i = 2; i <= n; i++) {
        if (isPrime[i]) {
            cout << i << " ";
        }
    }
    cout << endl;
}

int main() {
    int n;
    cout << "Въведете число: ";
    cin >> n;

    sieveOfEratosthenes(n);
    return 0;
}
```

## 🎯 Заключение

Темите за делимост, алгоритъма на Евклид, обикновените дроби и простите числа са основополагащи за разбирането на теорията на числата. Те намират приложение в множество области на математиката и програмирането, включително криптография, анализ на данни и алгоритмична оптимизация.
