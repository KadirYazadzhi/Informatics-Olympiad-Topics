# 🔢 Бройни системи: Теория и Преобразувания

Бройната система е начин за представяне на числа чрез краен набор от символи (цифри).

## 🏛️ Видове бройни системи
1. **Позиционни**: Стойността на цифрата зависи от нейната позиция (напр. десетична, двоична).
2. **Непозиционни**: Стойността на цифрата е фиксирана (напр. римски цифри).

## 🔄 Преобразуване между системи

### 1. От основа $P$ в десетична
Използваме полиномното представяне:
$$N = d_k P^k + d_{k-1} P^{k-1} + \dots + d_0 P^0$$
**Пример (1011 в двоична)**: $1\cdot 2^3 + 0\cdot 2^2 + 1\cdot 2^1 + 1\cdot 2^0 = 8 + 0 + 2 + 1 = 11$.

### 2. От десетична в основа $P$
Използваме последователно деление на $P$ и записваме остатъците в обратен ред.

### 3. Метод на Хорнер
Ефективен начин за преобразуване на низове в числа:
```cpp
long long toDecimal(string s, int base) {
    long long res = 0;
    for (char c : s) {
        int val = (isdigit(c) ? c - '0' : c - 'A' + 10);
        res = res * base + val;
    }
    return res;
}
```

## 🛠️ Двоична система в компютрите
- **Битове и Байтове**: 8 бита = 1 байт.
- **Допълнителен код (Two's Complement)**: Начин за представяне на отрицателни числа в двоична система.

---

## 🏁 Заключение
Познаването на бройните системи е критично за побитовите операции и за решаване на задачи, изискващи нестандартно броене.