# 🔢 Бройни Системи (Numeral Systems)

Бройните системи са начин за представяне на числа чрез символи. Компютрите работят в двоична система, но програмистите често използват шестнайсетична.

## 1. Основни Системи
*   **Десетична (Decimal, Base 10)**: Цифри 0-9. $123 = 1 · 10^2 + 2 · 10^1 + 3 · 10^0$.
*   **Двоична (Binary, Base 2)**: Цифри 0, 1.
*   **Осмична (Octal, Base 8)**: Цифри 0-7.
*   **Шестнайсетична (Hexadecimal, Base 16)**: Цифри 0-9, A-F (където A=10, ..., F=15).

## 2. Преобразуване

### 2.1. От Десетична към База $K$
Използваме деление с остатък.
1.  Делим числото на $K$.
2.  Записваме остатъка (това е последната цифра).
3.  Повтаряме с частното, докато стане 0.
4.  Резултатът е записаните остатъци в обратен ред.

```cpp
string toBase(long long n, int base) {
    if (n == 0) return "0";
    string chars = "0123456789ABCDEF";
    string res = "";
    while (n > 0) {
        res += chars[n % base];
        n /= base;
    }
    reverse(res.begin(), res.end());
    return res;
}
```

### 2.2. От База $K$ към Десетична
Използваме схема на Хорнер или просто сумиране на степените.
$"1A" (16) = 1 · 16^1 + 10 · 16^0 = 26$.

```cpp
long long toDec(string s, int base) {
    long long res = 0;
    long long power = 1;
    for (int i = s.length() - 1; i >= 0; i--) {
        int digit;
        if (isdigit(s[i])) digit = s[i] - '0';
        else digit = s[i] - 'A' + 10;
        
        res += digit * power;
        power *= base;
    }
    return res;
}
// Или вградената функция:
// long long val = stoll(s, nullptr, base);
```

## 3. Бързи преобразувания
Между бази, които са степени на 2 ($2, 8, 16$), може да се преобразува директно чрез групиране на битове.
*   **2 -> 16**: Групираме по 4 бита отдясно наляво. `1101 0101` -> `D 5` -> `D5`.
*   **16 -> 2**: Всяка цифра става 4 бита.
*   **2 -> 8**: Групираме по 3 бита.

## 4. C++ Манипулатори
```cpp
int x = 255;
cout << hex << x << endl; // ff
cout << oct << x << endl; // 377
cout << dec << x << endl; // 255 (връща към десетична)
```

## 5. Задачи
1.  **Codeforces 492C**: Vanya and Exams (изисква long long).
2.  **UVa 10018**: Reverse and Add (палиндроми).
3.  **SPOJ ADDREV**: Adding Reversed Numbers.

## 🏁 Заключение
Внимавайте с препълването при високи степени (напр. база 2 при дълги низове). За числа над 64 бита ползвайте BigInt.
