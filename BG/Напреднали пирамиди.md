# 🧱 Пирамиди: Обединяеми пирамиди (Mergeable Heaps)

## 👈 Лява пирамида (Leftist Heap)

Двоично дърво, което спазва heap property.
- **S-value (dist)**: Разстоянието до най-близкия `null` наследник.
- **Leftist Property**: $dist(left) \ge dist(right)$. Винаги "тежим" наляво.
- Пътят надясно е кратък ($O(\log N)$).
- **Merge**: Сливаме десните пътища. Ако свойството се наруши, разменяме ляво и дясно дете.
- Всички операции са $O(\log N)$.

## 📐 Skew Heap (Наклонена пирамида)

Саморегулираща се версия на Leftist Heap (без да пазим $dist$).
- При всяко сливане **винаги** разменяме ляво и дясно дете.
- Амортизирана сложност $O(\log N)$. Много лесна за имплементация.

```cpp
Node* merge(Node* a, Node* b) {
    if (!a || !b) return a ? a : b;
    if (a->val > b->val) swap(a, b);
    a->right = merge(a->right, b);
    swap(a->left, a->right);
    return a;
}
```

---

## 🏁 Заключение

Skew Heap е "Splay дървото" на пирамидите – гениално просто и ефективно. Използвайте го, когато ви трябва сливане.