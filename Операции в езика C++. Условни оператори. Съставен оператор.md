# ➕ Операции в езика C++. Условни оператори. Съставен оператор.

## Операции в C++
C++ предоставя широк набор от оператори, които могат да се използват за извършване на изчисления, сравнения, логически операции и други.

<table>
    <thead>
        <tr>
            <th>Тип оператор</th>
            <th>Пример</th>
            <th>Описание</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Аритметични</td>
            <td><code>+</code>, <code>-</code>, <code>*</code>, <code>/</code></td>
            <td>Използват се за базови математически операции.</td>
        </tr>
        <tr>
            <td>Логически</td>
            <td><code>&&</code>, <code>||</code>, <code>!</code></td>
            <td>Използват се за логически проверки и условия.</td>
        </tr>
        <tr>
            <td>Оператори за сравнение</td>
            <td><code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&gt;</code></td>
            <td>Сравняват две стойности и връщат <code>true</code> или <code>false</code>.</td>
        </tr>
        <tr>
            <td>Присвояване</td>
            <td><code>=</code>, <code>+=</code>, <code>-=</code></td>
            <td>Използват се за присвояване на стойности или промяна на променливи.</td>
        </tr>
        <tr>
            <td>Инкремент/Декремент</td>
            <td><code>++</code>, <code>--</code></td>
            <td>Увеличават или намаляват стойността на променлива с 1.</td>
        </tr>
    </tbody>
</table>


## Условни оператори

Условните оператори контролират изпълнението на кода въз основа на дадено условие.

### 📌 `if` условие
```cpp
if (условие) {
    // Кодът се изпълнява, ако условието е вярно
}
```

Пример:
```
int number = 5;
if (number > 0) {
    cout << "Числото е положително!" << endl;
}
```

### 📌 if-else условие
```
if (условие) {
    // Кодът се изпълнява, ако условието е вярно
} else {
    // Кодът се изпълнява, ако условието е невярно
}
```

Пример:
```
int number = -3;
if (number > 0) {
    cout << "Числото е положително!" << endl;
} else {
    cout << "Числото е отрицателно!" << endl;
}
```

### 📌 switch-case
```
switch (стойност) {
    case стойност1:
        // Код за стойност1
        break;
    case стойност2:
        // Код за стойност2
        break;
    default:
        // Код за други стойности
}
```

Пример:
```
int day = 3;
switch (day) {
    case 1: cout << "Понеделник"; break;
    case 2: cout << "Вторник"; break;
    case 3: cout << "Сряда"; break;
    default: cout << "Невалиден ден"; break;
}
```

### Съставен оператор

Съставените оператори се използват за по-удобно комбиниране на операции и присвоявания.

<table>
    <thead>
        <tr>
            <th>Оператор</th>
            <th>Пример</th>
            <th>Еквивалентен на</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>+=</td>
            <td>a += 5;</td>
            <td>a = a + 5;</td>
        </tr>
        <tr>
            <td>-=</td>
            <td>a -= 3;</td>
            <td>a = a - 3;</td>
        </tr>
        <tr>
            <td>*=</td>
            <td>a *= 2;</td>
            <td>a = a * 2;</td>
        </tr>
        <tr>
            <td>/=</td>
            <td>a /= 4;</td>
            <td>a = a / 4;</td>
        </tr>
        <tr>
            <td>%=</td>
            <td>a %= 2;</td>
            <td>a = a % 2;</td>
        </tr>
    </tbody>
</table>


### 🏁 Заключение

C++ предоставя мощни инструменти за работа с данни и контрол на изпълнението на програми. Съчетаването на оператори и условни конструкции позволява ефективно решаване на различни задачи. 🚀
