# 📦 STL Контейнери: Структури от данни

Standard Template Library (STL) предоставя готови, оптимизирани структури от данни.

## 📂 Основни категории

### 1. Последователни контейнери
- `std::vector`: Динамичен масив. Бърз достъп по индекс ($O(1)$).
- `std::deque`: Двупосочна опашка. Бързо добавяне в началото и края.
- `std::list`: Двойно свързан списък. Бързо вмъкване навсякъде, но бавен достъп.

### 2. Асоциативни контейнери (Сортирани)
- `std::set`: Множество от уникални елементи. Реализирано чрез червено-черно дърво.
- `std::map`: Ключ-стойност двойки.
- Операции: Търсене, вмъкване, изтриване за **$O(\log N)$**.

### 3. Неподредени контейнери (Hash-базирани)
- `std::unordered_set` / `std::unordered_map`.
- Операции: Средно **$O(1)$**.

## 🛠️ Избор на контейнер
- Нужен бърз достъп по индекс? $\to$ `vector`.
- Нужно е често добавяне/махане от двата края? $\to$ `deque`.
- Търсенето е критично? $\to$ `unordered_map`.

---

## 🏁 Заключение
Правилният избор на контейнер често определя дали програмата ще се впише в лимита за време. Познаването на сложността на всяка операция е задължително.