# 🕸️ Напреднали Графови Алгоритми

Тези алгоритми се базират на DFS и се използват за анализ на свързаността и структурата на графа.

## 1. Мостове и Артикулационни Точки (Bridges & AP)

В ненасочен граф:
*   **Мост**: Ребро, чието премахване увеличава броя на свързаните компоненти.
*   **Артикулационна точка (Cut Vertex)**: Връх, чието премахване увеличава броя на свързаните компоненти.

### Алгоритъм на Тарян (DFS Tree)
Използваме `tin[u]` (време на влизане) и `low[u]` (най-малкото `tin`, достижимо от `u` чрез поддървото му и едно обратно ребро).

1.  Започваме DFS.
2.  За всяко дете $v$ на $u$:
    *   Ако $v$ е родител, пропускаме.
    *   Ако $v$ е вече посетен (обратно ребро): `low[u] = min(low[u], tin[v])`.
    *   Ако $v$ не е посетен:
        *   `dfs(v)`
        *   `low[u] = min(low[u], low[v])`
        *   **Ако `low[v] > tin[u]`**: $(u, v)$ е **Мост**.
        *   **Ако `low[v] >= tin[u]`**: $u$ е **Артикулационна точка** (специален случай за корена: трябва да има > 1 деца).

## 2. Силно Свързани Компоненти (SCC)

В насочен граф, SCC е максимално подмножество върхове, където всеки е достижим от всеки.
Кондензацията на графа (свиването на всеки SCC в един връх) винаги дава DAG (Directed Acyclic Graph).

### Алгоритъм на Тарян за SCC
Използва стек.
1.  Пазим `tin[u]` и `low[u]`.
2.  Слагаме $u$ в стека.
3.  Когато обходим всички деца на $u$, ако `low[u] == tin[u]`:
    *   $u$ е "корен" на SCC.
    *   Вадим върхове от стека докато стигнем $u$. Всички извадени образуват един SCC.

## 3. 2-SAT (2-Satisfiability)

Имаме набор от булеви променливи $x_1, 
udots, x_n$ и условия от вида $(a 
ulor b)$, където $a$ и $b$ са литерали ($x_i$ или $
eg x_i$). Искаме да намерим стойности (True/False), удовлетворяващи всички условия.

**Свеждане до SCC:**
Условието $(a 
ulor b)$ е еквивалентно на $(
eg a 
u	o b)$ и $(
eg b 
u	o a)$.
1.  Строим граф на импликациите. Върховете са $x_i$ и $
eg x_i$.
2.  Добавяме ребрата за всяко условие.
3.  Намираме SCC.
4.  Ако $x_i$ и $
eg x_i$ са в един и същи SCC $
u	o$ **Няма решение** (противоречие).
5.  Иначе: Ако $topological_order(SCC(x_i)) > topological_order(SCC(
eg x_i))$, избираме $x_i = True$.

## 4. Задачи
1.  **SPOJ EC_P**: Critical Edges (Bridges).
2.  **SPOJ SUBMERGE**: Submerging Islands (Articulation Points).
3.  **CSES Giant Pizza**: 2-SAT.
4.  **Codeforces 427C**: Checkposts (SCC).

## 🏁 Заключение
`low-link` стойностите са ключът към разбирането на структурата на графа. Научете се да ги изчислявате правилно.
