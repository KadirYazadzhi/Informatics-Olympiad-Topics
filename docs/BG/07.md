# 🧮 Едномерни масиви: Памет и Управление

Едномерният масив е непрекъсната област в паметта, съхраняваща елементи от един и същ тип. В C++ масивите са изключително бързи поради директния достъп до паметта.

## 🧠 Вътрешно представяне и Памет
Когато дефинирате `int arr[10]`, компилаторът заделя `10 * sizeof(int)` байта (обикновено 40 байта).
- **Адресна аритметика**: Адресът на елемент `arr[i]` се изчислява като `BaseAddress + i * sizeof(type)`. Това става за $O(1)$ време.
- **Cache Locality**: Тъй като елементите са един до друг, процесорът ги зарежда в кеша едновременно, което прави масивите по-бързи от свързаните списъци.

## 🚀 Оптимизация на работата с масиви
1. **Предаване на функции**: Масивите винаги се предават по указател (pointer). Промените вътре във функцията променят оригиналния масив.
2. **Статични vs Динамични**:
   - Статични: `int a[100];` (в стека, фиксиран размер).
   - Динамични: `std::vector<int> v;` (в хийпа, променлив размер).

## 🛠️ Често срещани техники (Two Pointers)
Техниката на "двата показалеца" позволява решаване на задачи за $O(N)$ вместо $O(N^2)$.
- **Пример**: Намиране на два елемента със сума $S$ в сортиран масив.
```cpp
int i = 0, j = n - 1;
while (i < j) {
    if (a[i] + a[j] == S) return {i, j};
    if (a[i] + a[j] < S) i++;
    else j--;
}
```

## 🏁 Заключение
Масивите са основата на почти всички сложни структури от данни. Разбирането на тяхната работа с паметта е първата стъпка към писането на високопроизводителен код.