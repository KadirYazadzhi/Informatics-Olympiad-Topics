# 📐 Алгоритмична геометрия (Advanced II)

## 📏 Взаимно положение на точки и прави

Правата може да бъде представена по два начина:
1. **Общо уравнение**: $Ax + By + C = 0$.
2. **Параметрично уравнение**: $P = P_0 + t \cdot \vec{v}$.

### Разстояние от точка до права
За точка $M(x_0, y_0)$ и права $Ax+By+C=0$:
$$d = \frac{|Ax_0 + By_0 + C|}{\sqrt{A^2 + B^2}}$$

### Пресичане на две прави
За да намерим пресечната точка на две прави $A_1x+B_1y+C_1=0$ и $A_2x+B_2y+C_2=0$, решаваме системата линейни уравнения. Ако детерминантата $D = A_1B_2 - A_2B_1$ е 0, правите са успоредни.

---

## 🛑 Многоъгълници (Polygons)

### Площ на многоъгълник (Формула на Шнура / Surveyor's Formula)
За многоъгълник с върхове $(x_i, y_i)$:
$$Area = \frac{1}{2} | \sum_{i=1}^{n} (x_i y_{i+1} - x_{i+1} y_i) |$$
(където $x_{n+1}=x_1$ и $y_{n+1}=y_1$).

---

## 🛡️ Изпъкнала обвивка (Convex Hull)

Изпъкналата обвивка на множество точки е най-малкият изпъкнал многоъгълник, който съдържа всички точки.

### Алгоритми:
1. **Graham Scan**: Сортиране по ъгъл и използване на стек. Сложност $O(n \log n)$.
2. **Monotone Chain (Алгоритъм на Андрю)**: Разделя обвивката на горна и долна част. Сортиране по $x$. Сложност $O(n \log n)$.

---

## 🧭 Най-близки и най-отдалечени точки

### Най-близка двойка точки (Closest Pair of Points)
Решава се чрез **Разделяй и владей** (Divide and Conquer) или **Sweep-line** в $O(n \log n)$.

### Най-отдалечена двойка точки (Farthest Pair)
Най-отдалечените точки винаги са част от изпъкналата обвивка. Използва се методът на **Въртящите се калипери (Rotating Calipers)** в $O(n)$.

---

## 🛠️ Примерен код: Площ на многоъгълник
```cpp
struct Point { double x, y; };

double polygonArea(const std::vector<Point>& p) {
    double area = 0.0;
    int n = p.size();
    for (int i = 0; i < n; i++) {
        int j = (i + 1) % n;
        area += p[i].x * p[j].y;
        area -= p[j].x * p[i].y;
    }
    return std::abs(area) / 2.0;
}
```

---

## 🏁 Заключение

Геометричните алгоритми за многоъгълници и изпъкнали обвивки са класика в информатиката. Те изискват добро разбиране на векторните операции и внимателно боравене с точността на изчисленията. 🚀
