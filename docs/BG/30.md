# #️⃣ Хеширане и Хеш Таблици

Хеширането е метод за съпоставяне на данни (ключове) към числа (хешове) с цел бързо търсене ($O(1)$).

## 1. Хеш Таблици (Hash Tables)

Масив, в който записваме данните на индекс `hash(key) % size`.
Проблемът е **колизията**: когато два различни ключа дават един и същ индекс.

### Справяне с колизии
1.  **Separate Chaining**: Всяка клетка от масива пази списък (vector/list) с всички елементи, попаднали там.
2.  **Open Addressing**: Ако клетката е заета, пробваме следващата (Linear Probing) или друга формула.

## 2. `std::unordered_map` (C++)

Имплементация на хеш таблица.
*   **Сложност**: $O(1)$ средно, $O(N)$ най-лошо.
*   **Проблем**: Стандартната хеш функция за цели числа в GCC е идентитет (`x -> x`). Това позволява лесно създаване на тестове, които предизвикват $N$ колизии (напр. числа, кратни на размера на таблицата).

### Custom Hash Function
Винаги използвайте собствена хеш функция за `unordered_map` в състезания!

```cpp
struct custom_hash {
    static uint64_t splitmix64(uint64_t x) {
        x += 0x9e3779b97f4a7c15;
        x = (x ^ (x >> 30)) * 0xbf58476d1ce4e5b9;
        x = (x ^ (x >> 27)) * 0x94d049bb133111eb;
        return x ^ (x >> 31);
    }

    size_t operator()(uint64_t x) const {
        static const uint64_t FIXED_RANDOM = chrono::steady_clock::now().time_since_epoch().count();
        return splitmix64(x + FIXED_RANDOM);
    }
};

unordered_map<long long, int, custom_hash> safe_map;
```

## 3. GP Hash Table (PBDS)
По-бърза алтернатива от `unordered_map`.
```cpp
#include <ext/pb_ds/assoc_container.hpp>
using namespace __gnu_pbds;
gp_hash_table<int, int, custom_hash> table;
```

## 4. Хеширане на масиви/вектори
Ако трябва да пазим вектор в `unordered_set`, трябва да напишем хешер, който комбинира хешовете на елементите (напр. чрез `boost::hash_combine` или полином).

## 5. Задачи
1.  **Codeforces 4C**: Registration System.
2.  **CSES Sum of Two Values**: Класическа 2SUM с Map.

## 🏁 Заключение
Хеш таблиците са мощни, но опасни заради колизиите. Винаги ползвайте `custom_hash` с рандомизация (SplitMix64 + Time Seed).
