# 🔄 Цикли, Вложени цикли, Функции и Низове в езика C++

## 🔄 Цикли
Циклите в C++ позволяват изпълнението на един и същи блок от код множество пъти. Те се използват за работа с повторения и итерации върху структури от данни.

### Видове цикли:

1. **for цикъл**:  
   Използва се, когато броят на итерациите е известен предварително.

   ```cpp
   #include <iostream>
   using namespace std;

   int main() {
       for (int i = 0; i < 5; i++) {
           cout << "Итерация: " << i << endl;
       }
       return 0;
   }
   ```
   
2. **while цикъл**:
    Използва се, когато не знаем предварително колко итерации ще са нужни.
    ```cpp
    #include <iostream>
    using namespace std;
    
    int main() {
        int i = 0;
        while (i < 5) {
            cout << "Итерация: " << i << endl;
            i++;
        }
        return 0;
    }
    ```
    
3. do-while цикъл:
    Гарантира изпълнението на блока поне веднъж, дори ако условието е невярно.
    ```cpp
        #include <iostream>
        using namespace std;
    
        int main() {
            int i = 0;
            do {
                cout << "Итерация: " << i << endl;
                i++;
            } while (i < 5);
            return 0;
        }
    ```

## 🔁 Вложени цикли

Вложените цикли са цикли, които се намират вътре в други цикли. Те са полезни за работа с двумерни структури, като матрици.
Пример:
```cpp
#include <iostream>
using namespace std;

int main() {
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++) {
            cout << "i: " << i << ", j: " << j << endl;
        }
    }
    return 0;
}
```

Резултат:
```cpp
i: 0, j: 0
i: 0, j: 1
i: 0, j: 2
...
i: 2, j: 2
```

## 🔧 Функции в C++

Функциите позволяват разделяне на програмата на по-малки модули, което прави кода по-организиран и лесен за поддръжка.

### Дефиниране на функция:

```cpp
#include <iostream>
using namespace std;

// Дефиниране на функция
int add(int a, int b) {
    return a + b;
}

int main() {
    int result = add(5, 7);
    cout << "Резултат: " << result << endl;
    return 0;
}
```

### Видове функции:

- Функции без аргументи и връщана стойност:
  ```cpp
  void greet() {
      cout << "Здравей, свят!" << endl;
  }
  ```

- Функции с аргументи:
  ```cpp
  void printNumber(int num) {
      cout << "Число: " << num << endl;
  }
  ```

- Функции с връщана стойност:
  ```cpp
    int square(int x) {
        return x * x;
    }
  ```
  
## 📝 Низове

Низовете са последователности от символи. В C++ има два основни типа низове:

C-style низове (масиви от символи):
```cpp
#include <iostream>
using namespace std;

int main() {
    char str[] = "Здравей";
    cout << str << endl;
    return 0;
}
```

Стандартни низове (std::string):
```cpp
    #include <iostream>
    #include <string>
    using namespace std;

    int main() {
        string greeting = "Здравей, свят!";
        cout << greeting << endl;
        return 0;
    }
```

Основни операции с низове:

- Конкатенация:
  ```cpp
  string first = "Здравей";
  string second = " свят";
  string result = first + second;
  cout << result << endl; // Здравей свят
  ```

- Дължина на низ:
  ```cpp
  string text = "Програмиране";
  cout << "Дължина: " << text.length() << endl;
  ```

- Достъп до символи:
  ```cpp
  string word = "C++";
  cout << word[0] << endl; // C
  ```

## 🏁 Заключение

Циклите, функциите и низовете са основни компоненти на програмирането в C++. Разбирането и правилното им използване ще ви помогнат да изграждате ефективни и добре структурирани програми. 🚀
