# 📚 Стандартна библиотека STL: контейнери и итератори, основни алгоритми

## Увод в STL
Стандартната шаблонна библиотека (STL) е неразделна част от C++ и предоставя богат набор от компоненти за работа с данни. Тя включва контейнери, итератори и алгоритми, които се използват за ефективно управление на данни. 

**STL ключово се състои от три основни компонента:**

1. **Контейнери:** Основни структури от данни, които съхраняват данни по различни начини (масиви, листи, кутии, словници и др.).
2. **Итератори:** Метод за последователно преминаване през елементите в контейнери.
3. **Алгоритми:** Предимно шаблони за сортиране, търсене, проверка и др.


## Контейнери в STL

Контейнерите са средства за построяване на структури от данни и се разделят в три главни категории:

### 1. **Последователни контейнери**
Съхраняват елементи по пореден начин.

#### **Пример с `vector`:**
```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> vec = {1, 2, 3, 4, 5};

    // Добавяне на елемент
    vec.push_back(6);

    // Проведи се през елементите
    for (int val : vec) {
        std::cout << val << " ";
    }

    return 0;
}
```

### 2. **Асоциативни контейнери**
Съхраняват данни в ключ-стойностни двойки (напр. с `map`).

#### **Пример с `map`:**
```cpp
#include <iostream>
#include <map>

int main() {
    std::map<std::string, int> age;

    // Добавяне на данни
    age["Alice"] = 25;
    age["Bob"] = 30;

    // Извеждане
    for (const auto& [key, value] : age) {
        std::cout << key << ": " << value << "\n";
    }

    return 0;
}
```

### 3. **Контейнери с краяйно достъпване**
Основно се използват за специфични случаи, когато трябва да се работи със стекове (`stack`), опашки (`queue`) и двустранни опашки (`deque`).

#### **Пример с `stack`:**
```cpp
#include <iostream>
#include <stack>

int main() {
    std::stack<int> s;

    // Добавяне на елементи
    s.push(1);
    s.push(2);
    s.push(3);

    // Изваждане и извеждане
    while (!s.empty()) {
        std::cout << s.top() << " ";
        s.pop();
    }

    return 0;
}
```


## Итератори в STL

Итераторите са основен инструмент за достъп до елементи в контейнерите. Те абстрахират процеса на преминаване през елементите.

### **Пример с итератор:**
```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> vec = {10, 20, 30, 40};
    std::vector<int>::iterator it;

    for (it = vec.begin(); it != vec.end(); ++it) {
        std::cout << *it << " ";
    }

    return 0;
}
```


## Основни алгоритми

STL предлага множество алгоритми, които опростяват работа с контейнери. Те включват сортиране, търсене, манипулация и проверка на елементи.

### **Пример с `sort`:**
```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<int> vec = {5, 3, 8, 1, 2};

    std::sort(vec.begin(), vec.end());

    for (int val : vec) {
        std::cout << val << " ";
    }

    return 0;
}
```


## Заключение
STL е мощен инструмент за разработчици на C++. Разбирането на контейнери, итератори и алгоритми е от съществено значение за писане на ефективен и четим код.

