# 🧮 Дълги числа и Случайни числа

## 🧮 Реализация на дълги числа (BigInt)

Когато числата са по-големи от $10^{18}$, използваме низове за съхранение.

### Събиране на дълги числа
```cpp
string add(string a, string b) {
    string res = "";
    int i = a.size() - 1, j = b.size() - 1, carry = 0;
    while (i >= 0 || j >= 0 || carry) {
        int sum = carry + (i >= 0 ? a[i--] - '0' : 0) + (j >= 0 ? b[j--] - '0' : 0);
        res += to_string(sum % 10);
        carry = sum / 10;
    }
    reverse(res.begin(), res.end());
    return res;
}
```

---

## 🎲 Случайни числа

### Модерният начин (`mt19937`)
```cpp
mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
int x = uniform_int_distribution<int>(1, 100)(rng);
```
