# 🧮 Едномерни масиви и основни задачи с тях

Едномерните масиви са една от най-важните структури от данни, използвани за съхраняване и обработка на множество стойности от един и същ тип. В тази тема ще разгледаме подробно какво представляват, как се използват и как се решават основни задачи с тях.

## 📚 Едномерни масиви

### Какво е едномерен масив?

Едномерният масив е непрекъсната последователност от елементи, които могат да се достъпват чрез индекс. Индексите започват от 0 и се увеличават с 1 до размера на масива минус едно.

### Деклариране и инициализация на масиви

**Деклариране:**
```cpp
тип име_на_масив[размер];
```

Примери:
```cpp
int numbers[5]; // Масив от 5 цели числа
float values[10]; // Масив от 10 числа с плаваща запетая
```

Инициализация:
```cpp
int numbers[5] = {1, 2, 3, 4, 5};
float values[] = {3.14, 2.71, 1.41};
```

## 🔄 Основни задачи с едномерни масиви

1. Сумиране на елементите
    ```cpp
    #include <iostream>
    using namespace std;
    
    int main() {
        int numbers[5] = {1, 2, 3, 4, 5};
        int sum = 0;
    
        for (int i = 0; i < 5; i++) {
            sum += numbers[i];
        }
    
        cout << "Сума: " << sum << endl;
        return 0;
    }
    ```

2. Намиране на максимален и минимален елемент
    ```cpp
    #include <iostream>
    using namespace std;
    
    int main() {
        int numbers[5] = {3, 7, 1, 9, 4};
        int max = numbers[0];
        int min = numbers[0];
    
        for (int i = 1; i < 5; i++) {
            if (numbers[i] > max) max = numbers[i];
            if (numbers[i] < min) min = numbers[i];
        }
    
        cout << "Максимум: " << max << ", Минимум: " << min << endl;
        return 0;
    }
    ```

3. Сортиране на масив

Сортирането е процес, при който елементите на масива се подреждат в нарастващ или намаляващ ред.

## 🔍 Увод в алгоритмите за сортиране

Алгоритмите за сортиране играят ключова роля в обработката на данни. Най-често използваните методи са:

1. Bubble Sort
    ```cpp
    #include <iostream>
    using namespace std;
    
    int main() {
        int numbers[5] = {5, 3, 8, 6, 2};
    
        for (int i = 0; i < 5 - 1; i++) {
            for (int j = 0; j < 5 - i - 1; j++) {
                if (numbers[j] > numbers[j + 1]) {
                    swap(numbers[j], numbers[j + 1]);
                }
            }
        }
    
        for (int i = 0; i < 5; i++) {
            cout << numbers[i] << " ";
        }
        return 0;
    }
    ```

2. Selection Sort
    ```cpp
    #include <iostream>
    using namespace std;
    
    int main() {
        int numbers[5] = {5, 3, 8, 6, 2};
    
        for (int i = 0; i < 5 - 1; i++) {
            int minIndex = i;
            for (int j = i + 1; j < 5; j++) {
                if (numbers[j] < numbers[minIndex]) {
                    minIndex = j;
                }
            }
            swap(numbers[i], numbers[minIndex]);
        }
    
        for (int i = 0; i < 5; i++) {
            cout << numbers[i] << " ";
        }
        return 0;
    }
    ```

## 📝 Средства за работа с низове и търсене в тях

Какво е низ?
Низът е поредица от символи, които се обработват като единична текстова стойност. В C++ низовете могат да се обработват чрез масиви от символи или чрез класа string.

### Основни операции с низове

Пример за основни операции:
```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string text = "Hello, world!";
    cout << "Дължина на низа: " << text.length() << endl;
    cout << "Първи символ: " << text[0] << endl;
    cout << "Последен символ: " << text[text.length() - 1] << endl;
    return 0;
}
```

### Търсене в низове

Търсене на подниз:
```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string text = "Hello, world!";
    string search = "world";

    size_t found = text.find(search);
    if (found != string::npos) {
        cout << "Намерено на позиция: " << found << endl;
    } else {
        cout << "Поднизът не е намерен." << endl;
    }

    return 0;
}
```

### Сравнение на низове
```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string str1 = "apple";
    string str2 = "banana";

    if (str1 < str2) {
        cout << str1 << " е преди " << str2 << " в лексикографски ред." << endl;
    } else {
        cout << str2 << " е преди " << str1 << " в лексикографски ред." << endl;
    }

    return 0;
}
```

## 🎯 Заключение

Едномерните масиви, алгоритмите за сортиране и работата с низове са основополагащи концепции в програмирането. Те предоставят мощни инструменти за работа с данни, търсене и сортиране, които са в основата на много алгоритми и приложения.
