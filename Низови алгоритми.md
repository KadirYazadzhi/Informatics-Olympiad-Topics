# 📝 Низови алгоритми (Advanced)

## 🔍 Търсене на шаблон (String Matching)

### 1. KMP (Кнут-Морис-Прат)
Алгоритъм за намиране на всички срещания на шаблон в текст за $O(N + M)$. Използва **префиксна функция** (\(\\pi\)), която за всеки префикс на шаблона намира най-дългия му собствен суфикс, който е и негов префикс.

### 2. Z-алгоритъм
Намира **Z-функция** за всеки индекс $i$ – дължината на най-дългия подниз, започващ от $i$, който е и префикс на целия низ. Изключително полезен за задачи, свързани с префикси.

---

## 🏗️ Алгоритъм на Ахо-Корасик (Aho-Corasick)

Това е разширение на Trie, което позволява търсене на **множество шаблони** едновременно в един текст.
- Построява се Trie от всички шаблони.
- Добавят се "суфиксни връзки" (failure links) подобно на KMP.
- Позволява обработка на текста за $O(\text{length of text} + \text{matches})$.

---

## 📜 Компресиране на данни

### Кодове на Хъфман (Huffman Coding)
Алгоритъм за компресия без загуби. Приписва по-къси двоични кодове на по-често срещаните символи.
- Използва **алчна стратегия (Greedy)**.
- Построява се дърво, като на всяка стъпка се сливат двата възела с най-малка честота (използва се `priority_queue`).

---

## 🛠️ Манипулация на низове

1. **Палиндроми (Алгоритъм на Манакер)**: Намира всички палиндромни поднизове за линейно време $O(n)$.
2. **Суфиксни структури**: Суфиксен масив и Суфиксен автомат (разгледани в Тема 42).

---

## 🏁 Заключение

Низовите алгоритми са едни от най-красивите в информатиката. Комбинацията от Ахо-Корасик за множество шаблони и KMP за един шаблон покрива почти всички нужди при работа с текстови данни. 🚀
