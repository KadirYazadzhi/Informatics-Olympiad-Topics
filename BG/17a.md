# ⚙️ Алгоритми в STL

Библиотеката `<algorithm>` съдържа над 100 функции за работа с контейнери.

## 🔢 Сортиране и Търсене
- `std::sort(begin, end)`: $O(N \log N)$.
- `std::stable_sort(begin, end)`: Запазва реда на равните.
- `std::binary_search(begin, end, val)`: Връща `bool`.
- `std::lower_bound`: Първи елемент $\ge$ val.
- `std::upper_bound`: Първи елемент $>$ val.

## 🛠️ Манипулация на данни
- `std::reverse(begin, end)`: Обръща реда.
- `std::unique(begin, end)`: Премахва съседни дубликати (изисква сортиран масив).
- `std::next_permutation`: Генерира следващата подредба.

## 📊 Математически алгоритми
- `std::max_element` / `std::min_element`: Намира екстремумите.
- `std::accumulate(begin, end, init)`: Сумира елементите.

---

## 🏁 Заключение
Не преоткривайте колелото! STL алгоритмите са тествани и често по-бързи от ръчно написаните версии. Използвайте ги, за да намалите грешките в кода си.
