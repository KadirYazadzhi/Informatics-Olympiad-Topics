# 🎮 Теория на игрите в информатиката

## ♟️ Въведение в теорията на игрите

В състезателното програмиране често се срещат задачи за игри с двама играчи, които имат пълна информация, нямат елемент на случайност и играят оптимално.

### Основни понятия:
- **P-позиция (Previous)**: Позиция, от която предният играч (този, който току-що е направил ход) печели. Следващият играч е в губеща позиция.
- **N-позиция (Next)**: Позиция, от която следващият играч (този, който е на ход) печели.

---

## ⚖️ Стратегии за четност и симетрия

В много игри победната стратегия се основава на поддържането на определен баланс.

### 1. Стратегия на симетрията
Ако играта се играе върху симетрична дъска/структура, вторият играч често може да печели, като повтаря ходовете на първия огледално.
- **Пример**: Имаме кръгла маса и играчи поставят монети. Първият поставя в центъра, а след това вторият поставя точно срещу него.

### 2. Четност
Резултатът зависи от това дали броят на ходовете до края е четен или нечетен.
- **Пример**: Имаме купчина от $N$ камъчета и всеки взима по 1. Печели този, който вземе последното. Ако $N$ е четно, вторият печели; ако е нечетно – първият.

---

## 🎲 Комбинаторни игри и Играта Ним (Nim)

Играта Ним е класическа комбинаторна игра. Има няколко купчини с камъчета. На всеки ход играчът избира една купчина и взима произволен брой камъчета (поне едно) от нея.

### Теорема на Ним (Nim-Sum)
Позицията е губеща (P-позиция) тогава и само тогава, когато **XOR сумата** на размерите на всички купчини е равна на 0:
$$X_1 \oplus X_2 \oplus \dots \oplus X_k = 0$$

Ако XOR сумата е различна от 0, играчът на ход винаги може да направи ход, който да я направи 0.

---

## 🏆 Минимакс стратегия (Minimax)

За игри с ограничено пространство на състоянията (като морски шах, шах) се използва алгоритъмът Минимакс.

- **Maximizer**: Опитва се да максимизира крайния резултат.
- **Minimizer**: Опитва се да минимизира резултата на противника.

### Псевдокод:
```cpp
int minimax(Node n, bool isMaximizer) {
    if (n is leaf) return value(n);

    if (isMaximizer) {
        int best = -INFINITY;
        for (Node child : n.children)
            best = max(best, minimax(child, false));
        return best;
    } else {
        int best = +INFINITY;
        for (Node child : n.children)
            best = min(best, minimax(child, true));
        return best;
    }
}
```

---

## 🧠 Теорема на Спраг-Грънди (Sprag-Grundy)

Всяка комбинаторна игра, която завършва за краен брой ходове, може да бъде представена чрез едно число (Grundy value или nimber).
- $G(\text{state}) = \text{mex}(\{G(\text{next\_state}_1), G(\text{next\_state}_2), \dots \})$
- **mex** (Minimum Excluded value) е най-малкото неотрицателно цяло число, което не е в даденото множество.

Ако $G(\text{state}) > 0$, позицията е печеливша.

---

## 🏁 Заключение

Теорията на игрите изисква логическо мислене и често свеждане на сложна игра до проста математическа проверка (като XOR сума). Овладяването на стратегиите за симетрия и разбирането на Грънди числата дават огромно предимство в задачи от този тип. 🚀
