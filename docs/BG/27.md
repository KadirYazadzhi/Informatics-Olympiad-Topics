# 📐 Алгоритмична геометрия: Основи и Вектори

## 📏 Точка в многоъгълник (Point in Polygon)

Как да проверим дали точка $P$ лежи вътре в произволен (дори не-изпъкнал) многоъгълник?

### 1. Ray Casting Algorithm (Метод на лъча)
Пускаме хоризонтален лъч от $P$ надясно към $+\infty$.
- Броим колко пъти лъчът пресича страните на многоъгълника.
- Ако броят е **нечетен**, точката е **вътре**.
- Ако е **четен**, е **вън**.
- **Гранични случаи**: Внимаваме, ако лъчът минава точно през връх или съвпада със страна.

### 2. Winding Number (Число на въртене)
Сумираме ъглите, под които се виждат страните от точка $P$.
- Ако сумата е $2\pi$ (360 градуса), точката е вътре.
- Ако е 0, е вън.
- Този метод е по-стабилен, но по-сложен за имплементация (използва `atan2` или векторно произведение).

---\n

## 📐 Площ на триъгълник

### 1. Херонова формула
Ако знаем дължините на страните $a, b, c$:
$$S = \sqrt{p(p-a)(p-b)(p-c)}, \quad p = \frac{a+b+c}{2}$$

### 2. Векторно произведение (Shoelace за триъгълник)
Ако знаем координатите $(x_A, y_A), (x_B, y_B), (x_C, y_C)$:
$$S = \frac{1}{2} | (x_B - x_A)(y_C - y_A) - (y_B - y_A)(x_C - x_A) |$$
Това е по-прецизният метод, защото избягва квадратни корени и работа с `double` (ако координатите са цели).

---\n

## 🛠️ Вектори в C++

Винаги дефинирайте клас/структура `Point` с предефинирани оператори.

```cpp
struct Point {
    long long x, y;
    Point operator+(const Point& other) const { return {x + other.x, y + other.y}; }
    Point operator-(const Point& other) const { return {x - other.x, y - other.y}; }
    long long cross(const Point& other) const { return x * other.y - y * other.x; }
    long long dot(const Point& other) const { return x * other.x + y * other.y; }
};
```
- **Dot product (Скаларен)**: $A \cdot B = |A||B|\cos \theta$. Ако е 0, векторите са перпендикулярни.
- **Cross product (Векторен)**: Ако е 0, векторите са колинеарни.

---\n

## 🏁 Заключение

Избягвайте `sqrt`, `sin`, `cos` и `double` винаги, когато е възможно. Векторното произведение е най-добрият приятел на програмиста в геометрията – то работи с цели числа и дава точни резултати за ориентация и площ.