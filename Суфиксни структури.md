# 🔤 Формални езици и Суфиксни структури

## 🔤 Формални граматики и Автомати

В информатиката автоматите са модели за разпознаване на езици.
- **Крайни автомати (DFA/NFA)**: Използват се за редовни изрази (Regular Expressions).
- **Стекови автомати**: Използват се за контекстно-свободни граматики (напр. пресмятане на аритметични изрази).

---

## 📜 Суфиксен масив (Suffix Array)

Суфиксният масив на низ $S$ е сортиран списък на всички негови суфикси.
- **Пример**: За "banana", суфиксите са "banana", "anana", "nana", "ana", "na", "a".
- **Сортиран ред**: "a", "ana", "anana", "banana", "na", "nana".
- **Масив**: $[5, 3, 1, 0, 4, 2]$.

**Приложение**: Бързо търсене на подниз, намиране на най-дълъг общ подниз на два низа, преброяване на уникални поднизове.
- Построяване: $O(n \log n)$ или $O(n)$.

---

## 🌳 Суфиксно дърво (Suffix Tree)

Суфиксното дърво е компактно префиксно дърво (Trie), съдържащо всички суфикси на даден низ.
- **Алгоритъм на Укконен**: Позволява построяване за $O(n)$.
- **Свойства**: Позволява решаване на почти всяка задача за низове за линейно време.

---

## ⚙️ Суфиксен автомат (Suffix Automaton - SAM)

Суфиксният автомат е минимален детерминиран краен автомат, който разпознава всички суфикси на даден низ.
- **Сложност**: $O(n)$ памет и време за построяване.
- **Предимство**: По-лесен за имплементиране от суфиксно дърво и много мощен за задачи, свързани с поднизове.

---

## 🚀 Сравнение и избор

1. **Хеширане**: Най-лесно за кодене, риск от колизии.
2. **Suffix Array**: Относително лесно за кодене ($O(n \log^2 n)$ или $O(n \log n)$), памет $O(n)$.
3. **Suffix Automaton**: Най-бързо и мощно, но по-абстрактно за разбиране.

---

## 🏁 Заключение

Суфиксните структури са върхът на низовите алгоритми. Те превръщат сложни задачи за сравнение и преброяване на поднизове от $O(n^2)$ в $O(n)$. Владеенето им е задължително за задачи от международно ниво. 🚀
