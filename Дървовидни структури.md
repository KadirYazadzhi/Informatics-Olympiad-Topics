# 🌲 Дървовидни структури от данни

## 🌳 Двоични дървета за търсене (BST)

Двоичното дърво за търсене е структура, в която за всеки възел:
- Всички елементи в лявото поддърво са по-малки.
- Всички елементи в дясното поддърво са по-големи.

**Сложност**: Средно $O(\log n)$, но в най-лошия случай (дегенерирало дърво) – $O(n)$. Затова се използват балансирани дървета (AVL, Red-Black).

---

## 🧱 Пирамида (Heap / Binary Heap)

Пирамидата е пълно двоично дърво, което поддържа **Heap Property**:
- **Max-Heap**: Родителят винаги е по-голям от децата си.
- **Min-Heap**: Родителят винаги е по-малък от децата си.

**Приложения**:
- `std::priority_queue` в C++.
- Сортиране чрез пирамида (Heapsort).

---

## 🌳 Дърво на Фенуик (Fenwick Tree / BIT)

Дървото на Фенуик е структура, която позволява:
1. Актуализация на елемент ($O(\log n)$).
2. Пресмятане на префиксна сума ($O(\log n)$).

**Предимство**: Много лесно за кодене и пести памет.
```cpp
void update(int i, int delta) {
    for (; i <= n; i += i & -i) bit[i] += delta;
}
int query(int i) {
    int sum = 0;
    for (; i > 0; i -= i & -i) sum += bit[i];
    return sum;
}
```

---

## 🧱 Сегментно дърво (Segment Tree)

Сегментното дърво е универсална структура за **Range Queries**. То може да отговаря на въпроси за сума, минимум, максимум или НОД в произволен интервал $[L, R]$.

**Основни операции**:
- `build`: Построяване за $O(n)$.
- `update`: Промяна на елемент за $O(\log n)$.
- `query`: Заявка за интервал за $O(\log n)$.

### Пример за заявка за сума:
```cpp
int get_sum(int v, int tl, int tr, int l, int r) {
    if (l > r) return 0;
    if (l == tl && r == tr) return tree[v];
    int tm = (tl + tr) / 2;
    return get_sum(2*v, tl, tm, l, min(r, tm))
         + get_sum(2*v+1, tm+1, tr, max(l, tm+1), r);
}
```

---

## ⚡ Lazy Propagation (Мързеливо обновяване)

Техника при сегментните дървета, която позволява обновяване на **цели интервали** за $O(\log n)$. Вместо да обновяваме всеки лист, маркираме възела, че има "отложена" промяна за децата му.

---

## 🏁 Заключение

Дървовидните структури са гръбнакът на сложните алгоритми. Сегментните дървета и дърветата на Фенуик са задължителни за решаване на задачи с динамични промени в масиви и интервални заявки. 🚀
