# 📝 Низове и Търсене в текст

Низовете (strings) са последователности от символи. В състезателното програмиране те са източник на едни от най-трудните и интересни задачи.

## 🏗️ std::string в C++
Класа `std::string` автоматично управлява паметта и предоставя мощни методи:
- `s.size()` / `s.length()`: Дължина.
- `s += t`: Конкатенация (залепяне).
- `s.find(sub)`: Търсене на подниз ($O(N \cdot M)$).
- `s.substr(pos, len)`: Извличане на част от низа.

## 🔍 Алгоритми за търсене на подниз

### 1. Наивно търсене ($O(N \cdot M)$)
Проверяваме всяка възможна начална позиция. Достатъчно за малки низове.

### 2. KMP (Кнут-Морис-Прат) - Увод
Използва префиксна функция, за да избегне повторни проверки на вече мачнати символи. Позволява търсене за **линейно време $O(N)$**. (Разгледан детайлно в Тема 36).

## 🛠️ Полезни техники
- **Сравнение**: Низовете се сравняват лексикографски (като в речник). ` "apple" < "banana" `.
- **Stringstream**: Използва се за лесно парсване на думи от изречение.
```cpp
stringstream ss(sentence);
string word;
while (ss >> word) { /* обработка */ }
```

## ⚠️ Често срещани грешки
- **`s.find()` връща `string::npos`**: Винаги проверявайте дали резултатът не е равен на тази константа преди да го ползвате като индекс.
- **Извън границите**: Достъпът до `s[s.length()]` е невалиден.

---

## 🏁 Заключение
Низовете изискват внимание към символите и границите. За напредничави задачи се запознайте с полиномно хеширане и суфиксни структури.
