# 🌐 Алгоритми върху графи

## 🏗️ Представяне и обхождане

Графите се представят най-често чрез **Списък на съседство** (`vector<int> adj[N]`), тъй като е икономичен на памет и позволява бързо обхождане.

### Обхождания:
1. **DFS (Depth-First Search)**: Използва рекурсия (стек). Намира компоненти на свързаност.
2. **BFS (Breadth-First Search)**: Използва опашка. Намира **най-кратки пътища** в ненатегнат граф.

---

## 🛣️ Най-къси пътища в натегнати графи

### 1. Алгоритъм на Дейкстра (Dijkstra)
Използва се за графи с **неотрицателни** тегла. Използва приоритетна опашка.
- **Сложност**: $O(E \log V)$.

### 2. Алгоритъм на Белман-Форд (Bellman-Ford)
Работи и при **отрицателни** тегла. Може да открива отрицателни цикли.
- **Сложност**: $O(V \cdot E)$.

### 3. Алгоритъм на Флойд-Уоршал (Floyd-Warshall)
Намира най-кратките пътища между **всички двойки** върхове.
- **Сложност**: $O(V^3)$.
```cpp
for (int k = 0; k < n; k++)
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]);
```

---

## 🌳 Минимално покриващо дърво (MST)

Покриващо дърво е подграф, който свързва всички върхове без цикли и с минимална обща сума на теглата на ребрата.

### 1. Алгоритъм на Крускал (Kruskal)
Сортира ребрата по тегло и ги добавя, ако не образуват цикъл (използва **Union-Find**).
- **Сложност**: $O(E \log E)$.

### 2. Алгоритъм на Прим (Prim)
Започва от един връх и добавя най-близкия съседен връх.
- **Сложност**: $O(E \log V)$.

---

## ♾️ Специални пътища и цикли

### 1. Ойлеров път и цикъл
Път, който минава през **всяко ребро** точно веднъж.
- **Условие**: За цикъл – всички върхове трябва да имат четна степен. За път – точно два върха с нечетна степен.

### 2. Хамилтонов път и цикъл
Път, който минава през **всеки връх** точно веднъж.
- **Проблем**: Задачата е NP-пълна (няма бързо решение за общия случай). Използва се ДП с маски за малки графи.

---

## 🔄 Ориентирани графи (DAG)

В ориентираните графи е важно понятието **Топологично сортиране** – подредба на върховете така, че за всяко ребро $(u, v)$, $u$ е преди $v$. Възможно е само в ациклични графи (DAG).

---

## 🏁 Заключение

Алгоритмите върху графи са в основата на навигационните системи, социалните мрежи и оптимизационните задачи. Владеенето на Дейкстра и Крускал е критично за всеки състезател. 🚀
